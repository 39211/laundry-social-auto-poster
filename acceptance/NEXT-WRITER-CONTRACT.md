# 下一輪產品實作：核准內容的重跑、更新與撤回

現況是 W5 零公開接線可用，產品目前只通過本目錄六項驗收中的兩項。本目錄是獨立驗收輸入；Grok 實作者不得改寫它，也不得改基線的失敗為 PASS。

由 Claude commander 依現行共用入口安排新的隔離候選。Grok Native 明確指定 `grok-4.7-build-fast --reasoning-effort high`，或已驗的 Cursor `grok-4.7-high-fast`；不另購額度、不繞過 lease、不改共享派工預設。未實際派出不得記為已開工。

可寫範圍：候選 checkout 的 `src/seo90/`、`src/generatePublicSite.ts`、本功能相關 `test/` 與候選內新證據；仍由 generatePublicSite 擔任公開產物唯一 writer。測試產物限本次臨時根。其他範圍有必要時先提出精確差異，不能直接擴寫。

完成條件：

- 相同有效 release 再跑為無錯誤、無產物變動的 no-op。
- 同 URL 更新必須核准與 release 都對應新內容；文章、索引、服務頁與 sitemap 一致，原發表日保留，修改日正確。
- 撤回只移除本 release 擁有的產物和引用；其他文章、素材、服務頁核心及 sitemap 條目保留。最後一篇撤回不留下壞掉的 daily 導覽。
- 未重新核准的改稿不可破壞已公開版本。不得只刪 collision 守衛或吞掉錯誤來取得綠燈。
- 產物所有權、版本與來源 hash 可核對；先驗證與準備再替換，失敗保留舊版。補做失敗復原、同 slug 跨日期、junction/路徑逃逸與共享素材的必要測試；六項 gate 本身未完整覆蓋這些。

實作者交卷：候選 commit、精確差異、自測原始記錄與退出碼、固定 gate SHA 核對、六項 gate 的新報告、至少一項對外可觀測行為的反向驗證。不能以搜尋程式字串當行為測試。由另一位審查者獨立重跑及檢查接縫；同 GPT 家族審查不能標為跨家族。

不包括真核准、部署、社群發文、修復或補發老闆刪除的 FB/IG 貼文。真六篇仍是私稿，不能為測試捏造核准。即使全部通過，也只通過此功能契約，不能宣稱正式店家、搜索收錄或可販售驗收完成。
