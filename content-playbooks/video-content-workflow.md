# 私享家短影片與文案工作流程

## 目標

每週先從既有兩篇日更內容中挑出三個影片候選。候選必須有一個兩秒內看懂的記憶鉤子，並用一個單一、可拍、可驗證的動作完成內容；影片候選不會阻擋原定圖片貼文。

## 文案先決欄位

每個 `video_candidate` 必須包含：

1. `memory_hook`：觀眾隔天還能複述的短句。
2. `conflict`：畫面裡原本要發生什麼，以及為什麼突然停住或改變。
3. `single_action`：全片只有一個主要動作，避免人物、手部與物件穿模。
4. `payoff`：觀眾看完能立刻做的事。
5. `cta`：服務事實與下一步；目前固定為台中市全區免費收送、LINE 預約。
6. `first_frame_direction`：9:16 首幀的人物、物件數量、位置、受力與留白。
7. `grok_motion_prompt`：12 秒直式、固定鏡位、單一動作、明確終態與幾何負面條件。

## 每日穩定發布線

1. 06:30 重建當日內容、圖片與影片候選清單。
2. 圖片照原流程由 Codex 生成並逐張複審。
3. `video_candidate` 的存在不會把 `media_type=image` 改成 Reel。
4. 當天圖片通過核准後照 11:30／19:30 發布，不等待影片。

## 影片製作線

1. 鎖定文案記憶鉤子、衝突、單一動作與 CTA。
2. 先做 9:16 Codex 首幀；人物、手指、門框、提把、拉鍊、鞋帶與物件重量任一不合理就淘汰。
3. 帳號持有人在 SuperGrok 手動上傳核准首幀與 prompt，送出一次生成並下載原始 MP4。
4. 將 MP4 放到候選清單指定的 `formal_video_target_path`。
5. 依序通過 ffprobe、逐幀接觸表、首尾狀態、穿模、文字安全區與 CTA 複審。
6. 通過發布核准後才把該貼文升級為 Reel，並跑 `validate-publishable-media`。

## 不得升級 Reel 的情況

- 只有封面或流程預演，沒有正式 Grok MP4。
- 不是 9:16，或時長、H.264／AAC、公開 HTTPS URL 不合格。
- 動作超過一個，或手、人物、袋子、鞋子、門框出現穿模、增生、消失。
- 首幀與結尾狀態不符合劇本。
- 尚未完成 FB 與 IG 發布核准。

## 成效複盤

發布滿 72 小時後記錄實際 Reel 播放、三秒留存、完播、收藏、分享、LINE 點擊、詢問與預約。缺資料保留 `null`，不以發文數或觀看目標假設成長。下一輪只保留能提升記憶鉤子留存或實際 LINE／預約轉換的題材。
