# 驗收盤點 2026-08-16 13:40

> 老闆要求:整理出有問題的、會讓工作不順的地方,逐項附證據。
> 判準:每一條都有檔案或指令輸出可查,查不到的寫查不到。

## A. 會讓我沒辦法順利工作的(依危險排序)

### A-1 🔴 兩個 session 同時寫同一批檔案(今天真的炸了)
- **證據**:HEAD 上 `postCurrentSlot.ts` import 了三個不存在的匯出。另一 session 在 01:40–06:30 之間斷掉,改到一半的 postCurrentSlot **被我自己的 d0c82e8 掃進主線**(機制更正見 A-2)。`tsx` 在執行期解析 named import → **發布程式直接 crash,今天到 13:30 一則都沒發**(slot 2/3 十點多就核准了,每次補跑都死在 import)。
- **已修**:13:30 補齊三個匯出(照死掉 session 留下的測試規格),276 測試綠,slot 3 已在窗口內發出。
- **殘留風險**:只要兩個 session 還會同時開在這個 repo,同型事故就會再發生。**write-set 不重疊是鐵則,但這裡沒有 hook 強制。**

### A-2 ~~排程的 Pages commit 會把 src/ 掃進主線~~(14:30 查證後撤回:診斷錯誤)
- **原判**:自動 commit 掃整樹,把半成品帶進 HEAD。
- **查證推翻**:`81d85e7` 只含 docs/(79 檔零 src);`publishPages.ts:246` 的 commit 本來就是 pathspec 限定;06:30 另有 stash 守衛會把未提交的 src/scripts 修改暫存。**保護早就存在。**
- **真正的事故鏈**(證據:stash@{0}、d0c82e8 內容):
  1. 另一 session 同時改 postCurrentSlot + imageStamp + logging(01:40)
  2. **我把它改到一半的 postCurrentSlot 掃進我自己的 d0c82e8**(staging 後沒重跑 typecheck)
  3. 06:30 stash 守衛照設計把它未提交的 imageStamp/logging 暫存 → HEAD 的 import 失去定義 → 發布 crash
- **教訓**:錯不在自動 commit,在**我 commit 了一個別人正在動的檔案而沒有在 staging 之後重新驗證**。已入踩坑簿 F11。
- **佇列第 1 項(鎖 docs/)作廢** —— 要防的東西已經有兩層在防。

### A-3 兩份排程各說各話(第三次同種病)
- 時刻表(19:30/20:30)→ 已修。前綴表(可收藏)→ 已修。**這次是 REEL_SCHEDULE vs ab-test-plan**:產線照前者天天產新片,發布照後者輪播舊 12 支 —— **新片天天產、從來沒上檔**。
- **已修**:8/17 起 noon 直接跟隨 REEL_SCHEDULE;七日內零重複(驗證輸出在 commit)。

## B. 你點名的重複問題(證據 + 現況)

| 你看到的 | 證據 | 現況 |
|---|---|---|
| 影片重複 | `leather-shoe-rain` 8/11 發過、8/15 又發(同檔同文案);8/19 原本排了 8/14 才發的 `leather-bag-corner` | ✅ 8/17 起全是新概念(麂皮/跟頭/皮帶/保潔墊/毛毯/牛仔褲),七日內零重複 |
| 文案重複 | Reel 文案由概念決定,同概念重播=逐字同文案 | ✅ 新概念=新文案;**殘留**:21 天後同概念重播仍是同文案,需要每次播出重寫敘述(入佇列) |
| 圖片重複/文不對圖 | 今天 slot 1 圖是帆布鞋、文案是棉被(預產圖遇上改題) | 閘門有擋;圖正在重生;**根治**要「改題自動重生圖」(A7 產生端,入佇列) |

⚠ **新概念影片需要你看片**(唯一人工步驟):看片紀錄綁影片雜湊,新片=新雜湊=舊紀錄無效。沒看過的新片只會發封面圖,不會發影片。

## C. 六條線狀態

| 線 | 狀態 | 證據 |
|---|---|---|
| GA4 | ✅ 運作中,數字進 22:50 通知 | 8/14=14 clicks(post 11/ig-comment 3);8/15 領據 ledger |
| 索引 | ✅ 雙保險(06:30 + 補跑鏈補洞) | 8/15 補推 91 URL;`indexing-push-*.json` 連續 |
| SEO | ⚠ 六顆 SEO commit 已救(rescue 分支)**未合併**;錢頁重建、自訂網域準備都在裡面 | `rescue/worktree-seo-20260811` |
| FB/IG | ✅ 今天 slot 3 已發;slot 1 重生中;slot 2 20:35 | posted-log |
| 每日優化 | ✅ 23:10 檢查器活著,0 findings 起點 | 8/15 report |
| 複審 | ⚠ 三席跑完,**修完 14/17 條**;剩:快照防篡改(luna D6)、鞋類矛盾檢查擴家族(grok D3)、21 天後重播的文案新寫 | 兩份 RESULT.md |

## D. Loop 佇列(2 小時一輪,依序消化)

1. 發 slot 1(圖生完→核准→窗口內發)/ 確認 slot 2 20:35
2. 自動 Pages commit 範圍鎖 `docs/`(A-2)
3. `contradictorySubject` 擴到包/被/衣家族(grok D3)
4. 合併 rescue 分支的六顆 SEO commit(先逐顆 diff 對照現行主線)
5. aiwork-sxj 的 90 天營收計畫合併(對照表已有)
6. 同概念重播的文案變寫機制(21 天冷卻到期前完成)
7. A7 產生端:改題自動重生圖
8. 快照防篡改(原子寫+完整性,luna D6)
9. 每輪:讀 23:10 findings 當工作單、驗當日發布真的出去了
