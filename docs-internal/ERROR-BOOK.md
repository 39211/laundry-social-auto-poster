# 私享家產線踩坑簿(每個都真的發生過)

> 規則:每踩一個新坑,**當天**加一條。格式固定:`症狀 → 真因 → 修法 → 怎麼再驗一次`。
> 這份是給接手者看的。**動手前先掃一遍與你任務相關的段落**,不要用別人的血再流一次。

---

## A. 內容正確性(最貴的坑,會直接發錯東西給客人)

### A1|改了主題卻沒改圖片提示詞 → 文不配圖上線
- **症狀**:貼文寫「書包背帶」,圖是帆布鞋。
- **真因**:我改了 `topic` 和 captions,忘了 `image_prompt` 與 4 張輪播的 prompt 是獨立欄位。
- **修法**:改主題必須**同時**改 `image_prompt` + 每個 `carousel_items[].image_prompt`,刪舊圖、刪 day-lock、重生圖、**親眼看圖**、重鎖。
- **再驗**:生成後開圖看,不是看檔案存在。已加「文不配圖閘」(manifest fail-closed),但閘擋的是缺圖,**擋不了畫錯東西**——只有人眼能擋。

### A2|已存產出陷阱:改了生成器,行事曆還是舊內容
- **症狀**:改完 prompt 規則,產出完全沒變。
- **真因**:當日行事曆 JSON 已經存在,裡面帶著**上一版產生的成品文字**,生成器不會覆蓋已存欄位。
- **修法**:改生成器後,要嘛刪當日行事曆重生,要嘛直接改行事曆裡的欄位。踩過三次。
- **再驗**:改完後 grep 行事曆確認新規則的特徵字串真的出現。

### A3|舊 day-lock 把新內容打回去
- **症狀**:明明改成書包,隔天變回床組。
- **真因**:day-lock 是「當日內容正本」,heal 機制會拿鎖去覆蓋行事曆。鎖是舊主題,就把新的救回舊的。
- **修法**:**刻意重做流程** = 先刪 `data/day-locks/<date>.json` → 改內容 → 驗圖 → `npm run day-lock -- --date <date>` 重鎖。
- **再驗**:重鎖後比對 `day-locks/<date>.json` 的 topic 與行事曆 topic 一致。

### A6|閘門問的證人可以改口供:重跑 manifest 就把紅燈變綠燈
- **症狀**:8/14 slot 1 被文不配圖閘正確擋下(圖是帆布鞋、文案是白鞋)。我跑了 `generate-image-manifest` 想看新提示詞,**沒有重生任何一張圖**,閘門就變綠了。硬碟上還是帆布鞋。
- **真因**:閘門拿「行事曆的 topic」比對「image-prompts manifest 的 topic」,而 **manifest 是隨時可以重建的檔案**。一份寫在圖之後的文件,沒有資格替那張圖作證。
- **修法**:加第二個證人——`mark-image-source` 在**寫檔的當下**把 topic 蓋在每個檔案的來源紀錄上,閘門改比對它;沒蓋到就當作未證明(fail closed)。兩個證人都要過。
- **再驗**:三態突變實測(`test/imageTopicWitness.test.ts`)——主題不符→擋並指名檔案、沒有主題→擋、主題相符→不擋;把新迴圈拿掉,3 條裡 2 條變紅。
- **邊界(2026-08-14 三席複審後改寫,原本那段自己就寫錯了)**:
  原文說「擋的是自動化失效,擋不了人手動重跑」。**兩個方向都不準**——
  ①**高估**:同屬自動化的 A1 路徑照樣全綠(改主題與文案、忘了改 `image_prompt`、
  刪圖、`generate-missing-images.ps1` 依過期 prompt 重生並重蓋章)。
  蓋的是**當下行事曆的 topic**,不是圖片實際被什麼提示詞生出來的。
  ②**低估**:繞過面不只「手動重跑」,還有 `approve-post`(零圖文檢查直接寫核准)、
  核准後換圖(補跑鏈見到 approved-log 就不重審)、slot 2/3 完全不檢查。
- **仍未關的**:要真正成立,蓋章必須同時帶 **PNG sha256** 與 **prompt hash**,
  且只在位元變更時才准改;檢查要套到**每一格、每一張**。
- **通則**:🔴 **問閘門「你的證據是誰寫的、什麼時候寫的」。** 證據若能在被驗的東西之後被重寫,它證明的是那份文件,不是那個東西。
- **通則二**:🔴 **自己寫的「邊界宣告」也要被審。** 我以為誠實列出限制就夠了,
  結果那段宣告同時高估與低估——**它讀起來像克制,實際上是另一種沒查證的斷言**。

### A9|我的測試只測讀的那一半,寫的那一半刪掉還是全綠
- **症狀**:grok 設計的突變——把 `markImageSource` 裡 `topic: dailySlot.topic` 整行刪掉——**213 條測試全綠**。整個蓋章機制可以消失而沒有任何一條測試發現。
- **真因**:`test/imageTopicWitness.test.ts` 用 `writeSource()` **手寫 JSON** 餵給閘門,從頭到尾沒有呼叫過真正的 `markImageSource()`。我測的是「閘門讀得對不對」,不是「章有沒有被蓋上去」。
- **更糟的一條**:原本三則測試**從不寫 `data/image-prompts/<date>.json`**,所以第一證人永遠走 `catch`。也就是說**事故本身的形狀(manifest 已對齊、檔案還是舊的)沒有任何測試**。grok 設計的 3b 探針補上後,把第二證人搬進 `catch` 的突變才會變紅。
- **再驗**:三個突變的預測與實測**逐項相符**(2 紅 1 綠 / 3 綠 / 3a 綠 + 3b 紅)。
- **通則**:🔴 **一個機制有「寫」和「讀」兩側,測試要能分別殺死它們。** 只用手寫 fixture 餵讀側,寫側就是零覆蓋——而且看起來覆蓋率很好。問法是:**「把產生這份資料的那段程式刪掉,測試會紅嗎?」**

### A7|圖片在 06:30 生成、文案在 10:20 改掉,沒有人負責讓它們重新對上
- **症狀**:8/14 slot 1 圖文不一致,整個上午的補跑鏈(11:35/12:05/13:30/13:50)都沒發出去,當天只出了一則。
- **真因**:圖片是照 06:30 的 manifest 生的,主題之後變了,而**沒有任何一步會因為主題改變去重生圖片**。閘門正確擋下,但擋下之後沒有自動修復路徑,只能等人。
- **修法(當日)**:重生 manifest → 移走舊圖 → `scripts/generate-missing-images.ps1` → 重新核准 → `ALLOW_OFF_SCHEDULE_PUBLISH=true` 補發。
- **未修**:自動修復尚未存在。閘門會擋,但沒人會修——**這是目前最可能讓某一天只出半天內容的路徑**。
- **通則**:**擋下來不等於救回來。** 每個 fail-closed 閘門都要問「擋下之後,誰負責讓它變綠」;沒有答案就等於把停機時間交給運氣。

### A8|「已經有了就跳過」的判斷條件寫錯,規則就永遠不會套用
- **症狀**:8/14 中午發的 Reel 文案裡沒有可點的 LINE 連結、沒有價格、四個 hashtag 沒有一個是地區標。同一天的圖文貼文三樣都有。
- **真因**:兩層。①`scheduleReel.ts` 自己組文案,完全沒走 `contentPlan` 的共用規則(聯絡行/價格行/hashtag 階梯);②就算走了也沒用——`withLineContact` 的跳過條件是 `caption.includes("0968327653")`,而 Reel 文案裡寫的是「加 LINE 直接問:0968327653」,**號碼在、連結不在**,規則自己判斷「已經有了」就跳過。
- **後果**:GA4 的 `line_click` 是靠那個轉址連結記的。**每一支 Reel 都是不可量測的**——而 Reel 正是每天唯一穩定出的內容。花力氣把讀取端建起來,寫入端卻對一半的產出不生效。
- **修法**:①匯出 `withSharedCaptionRules(caption, topic)`,Reel 文案送進去;②跳過條件改判**轉址網址**而不是電話號碼。
- **再驗**:突變實測——把 Reel 那兩行改回不套用,`test/abTestPipeline.test.ts` 立刻變紅(缺地區標);測試同時斷言轉址網址必須在。
- **通則**:🔴 **「這個規則有沒有已經套用過?」的判斷,要認規則本身的產物,不要認一個近似特徵。** 電話號碼不是可點連結,而拿號碼當「有聯絡方式了」的證據,等於讓最弱的形式取消最強的形式。與 [A6] 同源:**證據要能證明你真正在意的那件事**。

### A10|閘門要求的證據,產線寫手交不出來 = 排定好的停機
- **症狀**:我把圖文舉證要求套到每一格之後,兩席複審同時指出:10:20 的 `daily-approve.ps1` **先跑 `heal-reel-slot` 再跑 auto-approve**,而 `scheduleReel` 寫的封面紀錄只有五個欄位(沒有 topic、沒有雜湊)。閘門會把它讀成「未證明」→ Reel 格被擋 → 當天只核准 slot 1 → 補跑鏈見到 approved-log 就不再重審 → **Reel 一整天發不出去**。
- **真因**:我加嚴了「要求」,卻沒同步升級**產線實際使用的那個寫手**。閘門與寫手是一組的,只動一邊就是把停機排進行事曆。
- **修法**:Reel 封面改走 `markImageSource`(帶 topic + 兩個雜湊),並自己補寫 manifest 條目(manifest 06:30 從行事曆生成,10:20 才 heal 進來的封面不在裡面)。
- **再驗**:還原成五欄寫法 → 測試變紅。
- **通則**:🔴 **加嚴閘門前,先列出「有哪些寫手會產生要被檢查的東西」,逐一確認它們交得出新證據。** 一個產線交不出證據的閘門,不是閘門,是停機。

### A11|只有一半的讀者認得的旗標,等於沒有旗標
- **症狀**:`data/ab-test-plan.json` 從 7→3 減量那天就寫了 `paused: true`,但**只有 PowerShell 產線讀它**;TypeScript 的 `planSlot` 照發不誤,所以 heal 會把已經退場的晚間 Reel 塞回當天。
- **真因**:旗標寫進資料檔,卻沒寫進型別、也沒進到所有讀取路徑。「暫停」在一個讀者眼中成立、在另一個眼中不存在。
- **修法**:`planSlot` 對 paused 直接回 `undefined` —— **暫停的半場不是「換個概念的半場」,是根本沒有這個半場**,否則每個呼叫端都得記得自己檢查。
- **再驗**:忽略 paused → 測試變紅。
- **通則**:**一個決定要在所有讀者眼中同時成立,才叫決定。** 加旗標時先數清楚有幾個讀者。

---

### F17|主圖驗了,slides 沒驗 —— 三天的輪播是隨機圖庫(老闆親抓)
- **症狀**:8/17 老闆看 FB 傻眼:「深色衣服三個位置」的輪播,四張是四件不同衣服(深T→米襯衫→藍襯衫);8/16 棉被組第三張變藍白毯。連三天,每天三組。
- **真因(三層)**:①輪播 slide 提示詞從未立「同一件實物」鐵律——Reel 線 8/11 就有的紀律,輪播線沒人移植;②我的「產完親眼看圖」只看主圖,slides 從未逐張驗;③閘門驗的是章與提示詞,像素永遠沒人看。
- **修法**:W-CAROUSEL 落地(物件護照+same-garment 強制+場景鎖+敘事鏡位,突變雙紅);驗收鐵則升級為**每一張**;視覺法官延伸管輪播(排程中)。
- **同日連帶教訓(時鐘,F16 加重)**:一小時內兩個方向心算錯時間(慌報 slot1 遲發、又把 10:17 當未來)。鐵則:**任何時間敏感結論,date 是第一條指令,工具輸出的時戳是校時源。**
- **通則**:🔴 抽查的樣本邊界=你的盲區邊界。「驗過主圖」給了我「驗過這組」的錯覺——觀眾看的是整組,驗收就要是整組。

### F16|把敘事時間當真實時間,對排程器發了假警報
- **症狀**:凌晨連續作業中,我以為當下是 07:20,判讀「Daily-Generate 06:30 漏火、Watchdog 停擺」並手動補發任務;實際本機時間 03:46,那兩個 next 時間都在未來——排程器完全正常。
- **真因**:時間感來自對話節奏的累積推算,不是查詢;途中多個工具輸出(vitest Start at 01:48/03:30)都印了真時間,我讀過去了。
- **後果**:無害(補發是冪等的,還順手把當日索引推早了);但若當時據此改排程設定,就是對正常系統動刀。
- **通則**:🔴 **任何「排程器沒跑」的結論,第一個指令是 date。** 長夜班的時鐘漂移是系統性的;工具輸出裡的時間戳不是裝飾,是校時源。

### F15|換片換出三個坑:legacy 預設槽、逐日連鎖、幽靈條目
- **症狀**:執行 Opus 裁定的 8/18 換片時連踩三坑:①`schedule-reel --date --concept` 直呼路徑預設 heal **晚間 slot 2**(legacy),把片塞進暫停中的槽;②從排程檔刪掉一個條目,loader 的「逐日連續」規則讓**後面 58 條全數被拒**;③換上的 wool-coat 被自己 8/16 的**幽靈條目**(從未實際上播的排程殘渣)以冷卻規則擋掉。
- **修法**:①壞 heal 的產物隔離,force 重生行事曆後用 `--heal` 走 plan 正路;②排程檔只能「換人」不能「抽日」——8/16 用唯一合法概念 heel-tip 填回(Python 全規則模擬先驗證 0 拒收才落盤);③幽靈條目=排程與現實的分歧,換人時先對 posted-log 查「實際播了什麼」。
- **再驗**:模擬器 60/60 接受、cooldown 測試 3/3 綠、heal 正確落在 slot 3、8/18 審核檔零自動蓋章(明午未審自動退封面)。
- **通則**:🔴 **改排程正本前先跑全規則模擬,不落盤試錯。** loader 的驗收規則(逐日連續+序列冷卻+相鄰異型)使任何手改都有連鎖半徑;「看起來只動一格」的編輯可以廢掉整條尾巴。另:CLI 的預設值是行為,不是備註——沒讀完 main() 就餵參數,等於閉眼開刀。

### F14|總指揮自己在 writer 執行窗內動了共享樹
- **症狀**:W-IGFIX(grok 實作線)交卷時 GATE=SCOPE_VIOLATION,指它寫了白名單外的 `video-production/enhancement-backlog.md`。
- **真因**:那個檔是**我**在它執行窗內(02:38)append+commit 的。門禁靠前後快照的全域 diff 歸屬寫入,窗內任何人動樹都算到 agent 頭上——CLAUDE.md 早就記過「兩個 writer 就無法歸屬(已誤報兩次)」,這是第三次,主犯是總指揮本人。
- **後果**:差點把一條乾淨的實作線記成違規退件(同 TaskKey 兩次實質失敗就升級換家——會冤枉 grok)。
- **修法**:收卷時先對時間軸再判責;歸屬給我、run 以三個白名單檔驗收通過。流程改為:**writer 在跑=樹全封**,我要上樹的東西(ERROR-BOOK/板況)一律排在「上一個 writer 收卷後、下一個點火前」的縫隙裡做完。
- **通則**:🔴 裁判不下場踢球,包括「只是順手寫個文件」——快照歸屬不認人,只認窗口。

### F13|驗收驗錯軸:字幕燒好了,故事沒對過
- **症狀**:8/16 suede 驗收件被老闆一眼退件:中段比 before 還髒(旁白明明說「那不是髒」)、鞋帶從米色變灰色、after 段鞋頭翻向。我送件前抽了四幀,但只驗了字幕位置、延遲、可讀性。
- **真因**:驗收清單只有技術軸(我這次改的東西),沒有內容軸(前中後是不是同一個物件、狀態順序是不是 壞→修→好)。增益量到 2.1「兩段不像同一場景」的大警報,被我當成要夾住的數字而不是要調查的訊號——夾住讓片子活下來是對的,但活下來的片要被標記,不是直接送驗收。
- **一個修法被自己打回**:光度連戲閘寫好後用真壞例校準——全幀均色與中央裁切**都分不開好壞例**(suede 差 18,健康白鞋反而差 37),假陰性直接刪掉不上線。鞋帶色、髒污紋理、方向是語意特徵,通道統計看不見。
- **修法**:`extract-reel-frames.ps1` 每支新片自動抽 4 幕劇情幀放片旁;loop 每輪用眼睛過故事軸;送驗收前必看幀;suede 壞素材隔離重生。
- **通則**:🔴 **驗收清單要從「觀眾會抱怨什麼」倒推,不是從「我改了什麼」正推。** 改了字幕就只驗字幕,是作者視角;觀眾看到的是整支片。另:一個沒有鑑別力的閘,上線比不上線更糟——校準要求「壞例必紅」不是形式,是它存在的理由。
- **【8/20 補記,F13 教訓的活體驗證】獨立第二席抓到我快速看漏的破綻**:同一天補審兩支新片,第一支(保潔墊,三幕三件不同物)我跟 grok 都一眼判 REJECT,沒有鑑別力可言;第二支(長夾邊角起毛)我自己過幀時**傾向 PASS**(「三幕都是深棕長夾、車線位置一致、故事順序合理」)——grok 逐幕做**地標追蹤**(hook 壞的是哪一個角、middle 手在處理的是哪一條邊、對不對得上)後判 REJECT:hook 是近端短邊角磨穿起毛,early/middle 手臂佔滿畫面、只露出**另一個完好圓角**在處理,close 是整只翻新對不上單點修補。回頭核對 2-early 幀,grok 是對的。
- **通則**:🔴 **「同一件物品」不能只看色澤材質這種粗特徵,要追蹤故事點名的那個具體地標**(哪個角、哪個邊、哪個位置)**在每一幕是否還在原地**。粗特徵一致 ≠ 地標一致,粗看正是我這次差點放行的原因。這也是雙審**設計本身**在起作用的證據——不是形式,是這次真的擋下一支會被觀眾抓包的片。

### F12|從未執行過的路徑,首次真實執行就是它的第一次測試
- **症狀**:8/16 是第一個治療 A 日,`Invoke-TreatedAssembly` 每次都 `produced no file`,看不到任何 ffmpeg 錯誤。這段程式 8/11 就寫好了,躺了五天。
- **真因(三層)**:①`"fontsize=$hookSize:fontcolor=white"` —— PS 雙引號字串裡變數後面直接接冒號,`$hookSize:fontcolor` 被解析成 scope 限定變數(同 `$env:PATH` 語法),不存在=展開為空,ffmpeg 收到 `fontsize==white` 直接炸,六處 drawtext 全中;②當天 suede 這對片實測增益 R 2.1047,超出 `colorchannelmixer` 的硬上限 [-2,2],filter 初始化整組失敗;③兩個錯都躲在 `2>&1 | Out-Null` 後面,外面只看得到「檔案沒生出來」。
- **修法**:六處改 `${hookSize}`/`${closeSize}`;增益解析處夾住 ≤2(寧可色彩略欠校正也不要沒有片);ffmpeg stderr 落地 `ffmpeg-treated.log`,throw 訊息帶最後 5 行。
- **再驗**:修完產線跑出 15s-tA+10s 控制組,兩支 `.subs.json` 都 `burned=true`;還原任一修法即重現原錯,錯誤訊息這次看得見。
- **通則**:🔴 **代碼寫好躺著 ≠ 能跑;「還沒執行過的分支」就是還沒測試過的分支。** 治療剪 8/12-14 三天都因機器睡著沒跑,大家以為它 ready,其實它從未活過。吞掉 stderr 的地方,「真 bug」與「檔案沒生出來」長得一模一樣 —— 診斷成本全轉嫁給下一個人。

### F11|把別人改到一半的檔案掃進自己的 commit,自己的測試綠救不了你
- **症狀**:8/16 整個上午發布 crash 在 import(缺三個匯出),slot 2/3 核准了發不出去,到 13:30 才有人發現。
- **真因**:並行 session 在改 postCurrentSlot+imageStamp+logging;我提交 d0c82e8 時把**它改到一半的 postCurrentSlot** 一起 staged(我的 typecheck 是在它後續修改落地**之前**跑的);它的 imageStamp/logging 還沒提交,06:30 的 stash 守衛照設計把那些暫存 → HEAD 的 import 指向不存在的定義。
- **陷阱疊加**:我先把鍋甩給自動 Pages commit(A-2 原判),查證後發現該 commit 是 pathspec 限定、stash 守衛也有效 —— **兩層保護都在,漏的是我自己的 staging 紀律**。
- **修法**:看到「檔案被並行修改」的提醒後要 commit 該檔,**必須在 `git add` 之後、`git commit` 之前重跑 typecheck**;或干脆不把該檔納入這次 commit。
- **通則**:🔴 **測試綠只證明「跑測試那一刻」的樹,不證明「你 commit 那一刻」的樹。** staging 與驗證之間每多一秒,並行寫入就多一秒的窗。

## B. 驗證方法本身出錯(最陰險:讓你以為壞了,或以為好了)

### B1|cp950 假陰性:中文比對「不報錯、直接 0 命中」
- **症狀**:驗證 YouTube 描述,回報「參考價/至善國中/儲值全都沒有」——實際上線上全都有。
- **真因**:內容經過 shell 管線/檔案落地時被 cp950 毀了,我拿**損壞的資料**去比對,Python 又用 `errors='replace'` 吞掉,毫無錯誤訊息。
- **修法**:中文比對**抓取與比對必須在同一個 Python 程序內完成**,不經過 shell 管線、不落地中間檔。
- **再驗**:同一份內容故意打錯一個字,比對必須變 False——比對器要能分辨真假,不然它只是在說「找不到」。

### B2|退出碼與自述不可信
- **症狀**:腳本 exit 0,但實際沒發文;或 exit 非 0,但其實發成功了。
- **真因**:發布鏈有多段,退出碼只反映最後一段。
- **修法**:發布真相**只看 `data/posted-log/<date>.json`**;YT 看 `data/youtube-log/`;結算看 `output/day-reports/`。
- **再驗**:任何「已完成」都要指出是哪個檔案的哪一筆記錄。

### B3|閘門自己壞掉時等於全放行(Grok 紅隊找到,最重的一條)
- **症狀**:PreToolUse 閘門拋例外 → exit 1;但攔截條件是 exit 2 → **全部放行**。
- **真因**:fail-open 預設。
- **修法**:閘門一律 fail-closed;例外路徑也要回攔截碼。
- **再驗**:故意讓閘門拋例外,確認它擋下來而不是放行。

### B4|檢查器搜的是「我在對話裡發明的名字」,不是程式裡真實存在的字
- **症狀**:驗證「船長方法有沒有真的進生產」,回報三幕相機節拍「文件無、生產無」——實際上它完整存在於 `scripts/produce-next-reel.ps1:508-519`。
- **真因**:我搜 `push-to-defect`／`hold-on-contact` 這類**簡稱**,那是我在對話裡為了溝通方便造的詞,程式裡寫的是完整英文指令(`a slow push-in that ends framed on the worn area`)。**簡稱只活在對話裡。**
- **修法**:查「某個概念有沒有被實作」時,先打開檔案看它**實際怎麼命名**,再用那個字串搜;或搜行為特徵(函式名、switch 分支)而不是概念名。
- **再驗**:對一個**已知存在**的功能跑同一套檢查,回 False 就是檢查器壞了,不是功能不見了。
- **與 B1 同源**:兩者都是「驗證方法本身出錯,而且錯得很有說服力」。

### A4|同一支影片配同一則文案,隔天再發一次(2026-08-07~11 發生四次)
- **症狀**:IG 上 8/10 12:01 與 8/11 20:31 是同一支皮鞋影片、同一則文案;窗簾、後背包同樣各兩次。觸及從 169 掉到 26-87。
- **真因**:`generateDailyContent.ts` 保留「已排定的 reel slot」時保留**整個 slot 物件**,文案跟著影片從前一天中午被帶到隔天晚上。程式碼註解只想保住影片檔,實際連文字一起保住了。
- **修法**:只保留媒體欄位(`media_type`/`local_video_path`/`topic`),文案永遠屬於當天;另加發布端查重閘(近 7 天有逐字相同且真的發布過就拒發)。
- **再驗**:突變實測——拿掉查重閘,`test/publishGuards.test.ts` 必須變紅(已驗證)。

### B5|dry-run 寫出空的指紋檔,反而讓指紋閘失效
- **症狀**:`data/approved-log/2026-08-12.fingerprints.json` 內容是 `{}`,而當天核准檔根本不存在。
- **真因**:dry-run 跳過核准紀錄卻仍寫指紋檔;而發布端寫 `if (expected && expected !== actual)`,**沒有條目就短路放行**。檔案存在 = 宣稱已指紋化,但實際不檢查任何東西。
- **修法**:指紋寫入移進 `if (!options.dryRun)`;發布端把「有指紋檔但缺這個 slot」改成**拒發**。
- **再驗**:突變實測——把缺項判斷改成永不成立,測試必須變紅(已驗證)。
- **通則**:**空的保護檔比沒有保護檔更危險**,因為它讓檢查看起來存在。

### B6|測試全綠不代表保護有效——刪掉保護仍然全綠
- **症狀**:199 個測試全綠,但跨家族審核把七天重複閘、文不配圖閘、指紋比對、單飛鎖、NonRetryableError、healDay 逐一刪掉重跑,**結果與基準完全相同**。
- **真因**:那些機制**根本沒有對應測試**(grep `fingerprint`/`dayLock`/`NonRetryableError` 於 test/ 零命中)。
- **修法**:寫測試時同時做突變實測——拿掉被保護的那一行,測試必須變紅;不會變紅的測試要刪掉,不要留著當防禦縱深。
- **實證**:我今晚新寫三條,其中一條(dry-run 不留痕)拿掉保護仍全綠,因為 autoApprove 在寫指紋前就先拋錯 → **已刪除該條**,不留假綠。

---

## C. 這台機器 / PowerShell 5.1

### C1|bash 的時鐘會偏移數小時
- **症狀**:以為是凌晨 3 點,實際上午 9:41,判斷全錯。
- **真因**:`TZ=Asia/Taipei` 在這台的 bash 會退回 GMT。
- **修法**:**時間一律以 PowerShell `Get-Date` 為準**。

### C2|heredoc 寫 Python 時反斜線被吃掉
- **症狀**:路徑 `data\run-locks` 變成 `data<CR>un-locks`。
- **真因**:`\r` 被當跳脫字元。
- **修法**:路徑用正斜線,或 `chr(92)` 明確組字;讀檔用 `newline=''`。

### C3|`ConvertFrom-Json` 管線形式在 5.1 會炸
- **修法**:用引數形式 `ConvertFrom-Json -InputObject $text`,不要 `$text | ConvertFrom-Json`。

### C4|Write/Edit 寫出的 `.ps1` 沒有 BOM,含中文直接語法錯誤
- **修法**:寫完補 UTF-8 BOM。

---

## D. 排程與併發

### D1|死觸發:排程存在但 NextRunTime 空白 = 永遠不會跑
- **症狀**:任務在清單裡,卻從來沒執行。
- **修法**:watchdog 已加偵測(NextRunTime 空 → 重新註冊)。人工檢查:`Get-ScheduledTaskInfo` 看每一條的 NextRunTime。

### D2|一次性觸發加上去會蓋掉每日觸發
- **症狀**:補一個 `-Once` 觸發後,每日排程消失。
- **真因**:`Set-ScheduledTask -Trigger` 是**取代**不是追加。
- **修法**:`-Trigger (@($t.Triggers) + $once)` 保留原有的。
- **再驗**:設定後列出 Triggers,確認 Daily 和 Once 兩條都在。

### D3|同一支腳本被兩個排程同時叫起 → 內容互相踩踏
- **修法**:單飛鎖(publish 用 date+slot,生產腳本用 run-lock,30 分鐘視為過期可回收),排程設 MultipleInstances=IgnoreNew。

---

## E. 對外 API

### E1|`media_publish` 之後絕不可重試
- **真因**:那一刻貼文可能已經建立,重試 = 重複發布。
- **修法**:`NonRetryableError`,網路中斷或 5xx 都不重試,改去查 posted-log 與後台實際狀態。

### E2|限動轉發 404:行事曆帶著影片網址,但影片其實沒產
- **修法**:`verifyPublicAssetUrl` 先驗,失敗就 fallback 用圖。

### E3|Meta token 貼成 User Token 會讓整條鏈失效
- **修法**:必須是 **Page Token**,用 repair-meta-token 修。

---

## F. 專案管理 / 我自己犯的

### F1|海報做成價目表(v1)、做成整齊卡片(v2)—— 兩次都被退
- **真因**:我把海報當「文件排版」,做了三個區塊平均分配。真海報是**一個絕對主角 + 極端比例對比**,3 公尺外要能讀。
- **修法**:先讀規則(單一焦點/視覺階級/前後對比版式)→ 出規格 → 找設計總監複審 → 才渲染。v3 = 滿版出血 62% + 標題咬圖 + 金色只給兩處。
- **教訓**:視覺產出**不要自己一版定稿**,先找複審。

### F2|一次改多個變數,數據就廢了
- **修法**:每批只動一個變數(節奏 or 鉤子 or 長度,不可同時)。人設上線也要避開 A/B 期間。

### F3|素材排程排到期末就斷,生產線找不到東西做
- **症狀**:A/B 計畫只排到 8/14,8/15 之後生產線空轉。
- **修法**:排程表要**永遠比生產視窗多兩週**;生產視窗 4 天(原本 3 天太緊,失敗一次就開天窗)。
- **再驗**:每週檢查 `data/ab-test-plan.json` 最後日期 ≥ 今天 +14。

### F4|不獨立驗證交回來的東西,等於沒驗
- **實證**:我自測 15/15 全綠的 hook,Grok 判 EXPLOITABLE(16 條攻擊)、Opus 判 18 個 DEFECT。
- **修法**:自測全綠只是最低門檻。會影響放行的東西(閘門/計分器/驗證器)一律再送深審+跨家族紅隊。

### F5|實驗被綁在「有沒有新東西要做」上,結果實驗跑在零素材上
- **症狀**:A/B/C 中段治療排在 8/12-14,但生產腳本只在「有待產概念」時才進入生產體。所有概念都已產完 → 治療組裝是死碼 → 三天後會得到「中段治療無效」。
- **真因**:治療是**重剪已有素材**,不需要新概念,卻寫在「找新工作」的分支裡。
- **修法**:治療日改為挑「當天計畫要播的概念」重剪;覆蓋標準檔名前先保留 `-untreated` 版(同概念之後還會再播)。
- **再驗**:實測選取回傳當天要播的概念與 variant,不是 null。
- **通則**:**實驗的觸發條件不可以跟生產的觸發條件共用**。得到「無效」時要先問「它到底有沒有被執行過」。

### F6|用絕對座標排版,元素會互相疊上去而且不會報錯
- **症狀**:海報 v4 第一版的 QR 白框直接壓在優惠塊文字上。
- **真因**:各元素用 `H - 固定值` 各自定位,沒有人負責檢查總高度。
- **修法**:改成由上到下累加的 mm 預算,存檔前 `assert` 總和與底部界線。
- **再驗**:故意把某段高度加大,assert 必須擋下來。

### F7|只算區塊高度、不算文字實際佔高,文字會壓在下一個元素上而不報錯
- **症狀**:海報 v5 第一版「儲值 1000 送 100」壓在「運動鞋 $250 起」上面。v4 也犯過同型(QR 壓優惠塊)。
- **真因**:我用「這一塊我給它 30mm」來排版,但 20.8mm 的中文字實際佔高約 26.6mm(字級 ×1.28),加上內距就溢出。**PIL 畫字不會抱怨越界。**
- **修法**:畫字的函式回傳「文字底部座標」,後續元素一律以它為起點;每個容器存檔前斷言內文底部 ≤ 容器底部。
- **再驗**:故意把字級調大 20%,斷言必須擋下來(v5 實測擋下了兩次)。

### F8|用眼睛決定字級,就會做出走過去才看得到的海報
- **症狀**:v3/v4 的副標 7.5mm、優惠 10mm —— 分別要走到 0.9m 與 1.2m 才讀得到,而海報貼在門口是給 2-3m 外的人看的。
- **真因**:沒有把「這個字要在幾公尺外被讀到」變成參數,只憑畫面比例挑大小。
- **修法**:字高 = 閱讀距離(m) × 8.33(業界規則:1 英寸/10 英尺);先宣告每個元素的目標距離,字級由公式產生。主圖高度是可以退讓的,字高不行。
- **再驗**:拿同一把尺回頭量舊版,失敗項要能被指認出來(已量,v3/v4 各三項不合格)。

### E4|「發布後絕不重試」只在單一行程內成立,補跑鏈照樣重發
- **症狀**:8/11 slot 2 IG attempts=2;圍堵令記載 dual-Reel publication。
- **真因**:三層洞。①FB 發布端(photos published:true、Reel finish、輪播 /feed)完全沒有 commit 保護,普通 Error → withRetry 重跑整條上傳;②IG 的 response.json() 在 try 外,回應在傳輸中斷線 = 可重試的 SyntaxError —— 正是它要擋的那種情況;③commit 失敗記成 "failed",補跑鏈把 failed 讀成「沒發過」→ 兩小時後再發一次。
- **修法**:兩平台 commit point 統一白名單制(只有明確成功可繞過,其餘一律 NonRetryable);新增 posted-log 狀態 "uncertain",hasRecordedPost 視為已記錄,人工查後台後才清除。
- **再驗**:突變實測 —— 把 uncertain 從 hasRecordedPost 拿掉,測試變紅(已驗)。
- **通則**:**「絕不重試」要在每一個會重新進場的路徑上成立**:行程內(withRetry)、跨行程(補跑鏈)、跨日(排程救援)。只擋一層等於沒擋。

### D4|機器睡著 = 整天靜默不發,而所有檢查都看不到
- **症狀**:2026-08-12、08-13 連續兩天一則都沒發。程式沒有任何錯誤、測試全綠、排程全部 Ready。
- **真因**:所有排程 `WakeToRun=False`。機器睡著時 Task Scheduler **不會叫醒它**,而 `StartWhenAvailable=True` 讓它「醒來後再補跑」——8/13 23:40 開機時所有錯過的任務一次全跑,隨即又被關機殺掉(`LastTaskResult=3221225786` = 0xC000013A,行程被強制終止)。發布視窗那時早就過了。
- **為什麼沒被發現**:夜檢只檢查「明天備妥了沒」,從來不問「**今天到底發出去了嗎**」。所有指標都在講未來,沒有一個看當下。
- **修法**:①發布鏈五個排程設 `WakeToRun=True` + 允許電池供電時執行;②夜檢加第 10 項:posted-log 沒有任何 success/posted 就判 HIGH,並直接檢查 WakeToRun 是否被改回 False。
- **再驗**:把 WakeToRun 改回 False,夜檢必須判紅(已加該檢查)。
- **通則**:**「排程存在且 Ready」不等於「它會在該跑的時候跑」。** 排程的健康度有三層:存在 → 有下次執行時間 → **環境允許它在那個時間執行**。前兩層我早就在檢查,第三層今天才補。
- **殘留風險**:WakeToRun 只能叫醒**睡眠**中的機器,**完全關機救不了**。這一條要靠人:機器晚上別關,或改用一台常開的機器。

### A5|物件顏色寫死在程式的分類表裡,主題說白鞋也會畫出深藍鞋
- **症狀**:2026-08-14 slot1 主題「白鞋鞋邊泛灰」,生成出來是**深藍色帆布鞋**,而且鞋邊是乾淨的白 —— 主題講的缺陷根本不在畫面上,背景還有洗衣機(風格正本明文禁止)。
- **真因**:`contentPlan.ts` 的輪播提示詞把物件寫成**每個分類一個常數**:`headline === "鞋子"` → `"navy-and-warm-white sneakers"`。**深藍是寫死在程式裡的**,主題再怎麼說白鞋都沒用。而提示詞其餘部分只有「保持特徵物件一致」,從頭到尾沒說出物件是什麼。
- **修法**:物件由**主題本身**推導(白鞋/帆布/皮鞋/靴/勃肯/運動鞋各自的具體描述),不再用分類常數。
- **再驗**:突變實測 —— 退回寫死深藍,`test/captionQuality.test.ts` 的新測試判紅(已驗)。
- **通則**:**分類不是物件。** 主題已經說了它指的是什麼東西,而它說的顏色通常就是整篇的重點。把顏色藏在分類表裡,等於讓程式覆蓋作者的意思。
- **閘門為什麼擋不住**:文不配圖閘只驗「圖存在」,不驗「圖對不對」。**這一關到今天為止只有人眼能過。**

### F9|只檢查垂直不檢查水平,文字會靜靜溢出容器右緣
- **症狀**:知識型海報的價格行超出綠色卡片右緣;修完後註腳又超寬 2.2mm。
- **真因**:F7 之後我加了「文字實際佔高」的斷言,但**只管垂直**。中文字寬因字級與字距而變,同一句話換個字級就會爆出去,而 PIL 一樣不會抱怨。
- **修法**:繪字函式接受 `maxw`,超寬即 assert 並印出「超出幾 mm + 是哪一句」。
- **再驗**:把註腳改回長版,assert 立刻擋下(實測抓到 2.2mm)。
- **通則**:**容器有兩個方向。** 補了一個方向的檢查之後要問「另一個方向呢」——2.2mm 是眼睛看不出來、印出來才發現的量。

### F10|版型模板化之後,字數限制才是真正的規格
- **症狀**:同一支海報腳本換成精品包/棉被兩個主題,主標超寬 15.2mm、註腳超寬 12.9mm、CTA 卡在綠卡邊緣,一次跑就撞三個。
- **真因**:v8 的字級是由「閱讀距離」推出來的(F8),**每個欄位因此有固定的可容納字數**,而我寫新主題文案時只想著文意順不順。白鞋主標 8 字剛好,寫成 11 字就爆。
- **修法**:文案跟著版型走 —— 主標 ≤10 字、註腳 ≤26 字、CTA ≤14 字(`scripts/build-poster-knowledge.py` 實測值,已寫進 `poster-spec.md`)。
- **再驗**:三次超寬全部是 F9 的 `maxw` 斷言擋下的,**沒有一次是我眼睛看出來的**。
- **通則**:**把一次性版面變成模板時,新的失敗模式是內容而不是程式。** 模板本身不會壞,壞的是塞進去的字——所以斷言要留著,不能因為「上次跑過了」就拿掉。

### B8|檢查器自己死在第 7 項,後面三項從來沒跑過(而且沒人知道)
- **症狀**:23:10 的 `nightly_optimize.py` 手動一跑就 `UnicodeDecodeError: 'cp950' codec can't decode byte 0x8b`,接著 `AttributeError` 整支中止。
- **真因**:兩層。①`subprocess.run(..., text=True)` 用**系統語系**解碼,這台是 cp950,PowerShell 吐出的 UTF-8 位元組直接炸;②`except (subprocess.SubprocessError, OSError)` **沒有涵蓋 UnicodeDecodeError**,例外穿出去把整支殺掉。
- **後果**:第 8(IndexNow)、第 9(線上 gtag 存活)、**第 10(今天到底有沒有發)** 全部沒跑。第 10 正是 8/12-13 連兩天零發布之後才加的那一條——**為了防止靜默失敗而加的檢查,自己靜默失敗了**。
- **修法**:所有子行程改走 `run()` helper(強制 `encoding="utf-8", errors="replace"`,任何例外回傳 `""`);兩個 PowerShell 探測加上「一行都沒回 = 也是 HIGH」,因為**空結果不等於健康**。
- **再驗**:突變實測——把當天 `posted-log` 移走再跑,第 10 項立刻報「今天一則都沒發出去」;移回來就消失。修好前整支 exit 1、印不出任何 finding;修好後 exit 0、印出 1 筆。
- **通則**:🔴 **稽核腳本要有「我跑完了嗎」的證據,不能只看有沒有噴錯。** 一支中途死掉的檢查器,和一支「什麼都沒發現」的檢查器,在排程紀錄裡長得一模一樣。任何探測「沒有回傳」都要當成 finding,不能當成通過。

### B9|永遠成立的 finding,會訓練你略過整份報告
- **症狀**:每晚自檢固定報 HIGH「明天的行事曆不存在」。但行事曆本來就是**當天 06:30 才生成**,所以 23:10 看明天永遠是缺的 —— 這條 HIGH 每一晚都成立,而且每一晚都不需要行動。
- **真因**:檢查的是「**設計上還不該存在的產物**」,不是「會產生它的機制還在不在」。
- **後果**:比沒有這條檢查更糟。這份報告是「早上壞掉」與「靜默零發布」之間唯一的東西,而一條永遠亮紅的項目會讓人學會整份跳過 —— 我自己今晚就把它連同旁邊那條真的 finding 一起讀過去了。
- **修法**:改成檢查 **06:30 的觸發器還在、而且叫得醒機器**。沒行事曆+觸發器健康 = 正常;沒行事曆+觸發器死了 = 真正的緊急,而它以前被埋在噪音底下。
- **再驗**:健康狀態下安靜;把 WakeToRun 判斷反轉,立刻報「叫不醒睡著的機器」。
- **通則**:🔴 **一條永遠為真的檢查,不是保守,是雜訊。** 每加一條 finding 就問:「**這條在一切正常時會不會亮?**」會亮就重寫,不要靠人腦記得忽略它 —— 人腦會連旁邊那條真的一起忽略。

### B7|`python` 在 PATH 上的那一支,不一定是有裝套件的那一支
- **症狀**:`python scripts/build-poster-knowledge.py` → `ModuleNotFoundError: No module named 'qrcode'`,但這支腳本前一天才成功產出海報。
- **真因**:這台機器有多個 Python;PATH 上的沒裝 qrcode/Pillow,有裝的是 `C:/Users/cyc39/AppData/Local/Python/pythoncore-3.14-64/python.exe`(Vault 協定指定的那一支)。
- **修法**:腳本開頭直接寫出該用哪一支絕對路徑;不要只寫 `python`。
- **通則**:「昨天可以跑」不保證「今天用同一個指令可以跑」——**可跑的是某一支直譯器,不是某個指令字串**。

## F18|2026-08-17 夜:指紋蓋錯對象 + 蓋章制度沒有生日
- **事故一**:slot2 20:30 被核准指紋閘擋下。追查:行事曆 10:33 寫定、指紋 10:35 蓋章、slot1/3 同一張表 11:30/12:22 都過閘,唯獨 slot2 的存章對不上盤上任何版本(data 與 docs 副本逐欄含鍵序全同)。10:35 那次核准到底讀了哪份(worktree fallback?EPERM 讀取抖動落到 docs?)未定案;**確定的是 loadDailyContent 有 data??docs 雙源 fallback,核准與發布這種決策讀取不該有靜默備援**。→ 下一張單:決策路徑鎖單源,fallback 只准報錯。
- **事故二**:救援路是死的——閘門說「重跑 auto-approve」,auto-approve 對已核准日「nothing to do」早退,不重蓋指紋。撤銷機制不存在。**修章=動核准紀錄,自動化不可自助,只能老闆蓋**(權限層也如此裁定,合理)。→ 產線需要一條「老闆親手重審單一 slot」的正規 CLI。
- **事故三**:W-DEFENSE-B 的防偽把「今天以後+無章=竄改+強制重建」,但 08-17/08-18 行事曆生在蓋章制度之前——若無采用日邊界,明晨 06:30 會把自己已核准的存量判成偽造重建(=已存產出陷阱穿著保全制服)。已修:CALENDAR_STAMP_ADOPTION_DATE=2026-08-19,雙向突變測試釘死。
- **旁枝**:shoeSubjectFor 的 `麂皮→suede shoes` 列拔掉全套仍綠(舊碼盲區,非本輪引入);兩個排程任務 19:30 後被不明之手停用(非我、非看門狗;catchup 自癒救回)——歸因待查。

## F19|2026-08-18 凌晨考古:8/12-13 兩個暗日,兩種死法
- **發現路徑**:老闆問「都按 90 天計畫嗎」→ 對照表用 IG Graph API 當存活鐵證(不信 posted-log)→ 兩天零發文露出。此前**無任何機制報警**,晾了五天。
- **8/12**:catch-up log 無、daily-approve log 無、posted-log 無 → 全機沉默,疑似機器未開。
- **8/13**:daily-approve log **有**(有核准)、catch-up log 無、IG 零 → 核准後發布端未動,補跑排程也沒跑——與 8/17 晚「兩排程被不明之手停用」同型(那次靠腳本自癒救回)。任務被停的歷史比想像長。
- **防再發(已落地)**:①每日進步帳已掛排程(23:40),「發布完整」欄位讓暗日**隔天早上就現形** ②看門狗自癒被停任務(8/17 實證)③每週對照表把 IG API 直查欄位常設化(待產品化,排 CAPLINE 之後)。
- **教訓**:發布類 log「檔案不存在」和「有檔案零筆」是兩回事;缺格偵測與稽核一律要有「存活鐵證」源(平台 API 直查),不能只信自家紀錄。

## F20|2026-08-18 晨:首航抓到三條魚(防線第一次真的咬人)
- **魚一(修好)**:generate-missing-images.ps1 的 `@($group)` 對 PSCustomObject 泛型 List 在 PS 5.1 拋「引數類型不相容」→ D5 的 QA 接線 06:30 首航全程靜默死。TS 測試層照不到 PS 函式(又一次「綁測試≠有鑑別力」)。修=`,($group.ToArray())`;已重放驗證。**PS 腳本的關鍵函式要有 PS 層 smoke**,排入日板。
- **魚二(當場救)**:8/18 slot1 存貨(8/16 舊碼生)hero=工裝外套、slide2=藍襯衫;重生第一輪(護照=泛用 coat)又抽出羽絨外套。**泛物件主題(外套/衣物)的護照推導只有類別詞,鎖不住款式**——手動加 OBJECT LOCK(米色棉質工裝外套+襯衫領+袖扣,禁羽絨禁襯衫)第三輪三軸全 PASS+人眼四幀通過。→ 護照表要補「泛物件家族→具體預設款式」列,排入日板。
- **魚三(待修)**:法官輪播模式會漏吐逐張 OBS 塊(axes 有、observations 無)→ D4 正確地 FAIL_CLOSED(missing_observation)。守門對、發射端要修:judge prompt 強制 OBS 輸出或重試一次。今天靠人眼補完整性,warn-mode 不擋發布。
- **總評**:昨晚上的防線(D4/D5+采用日邊界)首航就攔下一次真事故——輪播不同件在發布前 4.5 小時被截住,而不是又一座紀念碑。

## F21|2026-08-18 全日停發事故(根治完成)
- **鏈**:8/17 14:15 scheduleReel 用原始 writeJsonAtomic 把「載入時的 tampered 執行期旗標」烙進行事曆檔(當時 stamp 政權 13:36 剛上線、寬限 22:14 才生)→ 旗標在檔內自我延續 → 8/18 11:15 起每輪核准被 calendar_integrity 擋 → 三格全停 → 22:48 老闆發現 → 22:51 手術移旗 → 22:57 slot2 補發成功(FB+IG),slot1/3 窗口已死。
- **根治(b830564,三鎖+三突變)**:載入器無條件剝除磁碟上的執行期旗標;蓋章寫入器拒絕序列化它;scheduleReel 改走 writeDailyContent。
- **我的兩失職**:①承諾「盯 10:20/11:30」卻沒掛任何看守,瞎 11 小時(系統 11:15 有 Toast,無人讀)②搶救中兩次吞輸出跑核准、讀到 22:15 舊報告下錯結論(F12 三犯)。
- **紀律修正**:①發布窗哨兵已排程(Laundry-Publish-Sentinel 11:50/12:20/20:50,缺格自動補發+留痕)——「我會盯」永遠要翻譯成「已掛看守」②跑會寫報告的指令禁 Out-Null,讀報告先驗 mtime。
- **未了**:slot3 圖證舊案(記錄與圖不符)另單處理。

## F22|2026-08-19 對撞日(0/3→1/3)+ 警報第二次喊給空氣聽
- **對撞**:F21 深修線的 133 檔未提交修改活在生產樹上,其 fail-closed 閘(受控 runtime 未佈署即封鎖外部動作)從 10:20 起擋掉核准/發布/產片,全天 0 發;老闆 20:2x 拍板 B(讓路先發):工作 byte-for-byte 保存至分支 f21-deepfix-wip(62edc72,已推遠端)+交接檔,主線回驗證鏈,slot2 於 20:38 雙平台救回。**鐵則:深修永遠在分支;生產樹只跑已提交已驗證的碼。**
- **警報二進宮**:哨兵 11:50/12:20 都正確偵測缺格並開火,但只寫進沒人讀的 log——F21「有 Toast 沒人讀」的完全重演,發生在教訓落檔的隔天。已修:sentinel v2(scripts/publish-sentinel.ps1,版控+BOM):缺格→補發→複查→仍缺就 Toast「需要人工介入」;實測全鏈通過。**鐵則補強:警報的定義=到得了人眼,寫檔不算。**
- **我的白天失明**:又一次沒有任何看守能在 10:20/11:30 叫醒我(F21 教訓寫了沒做全)——靠哨兵 v2 的 Toast 補上人眼通道;session 層的白天看守待補(排程喚醒不可用時只剩 Toast 通道)。
- **新系統病(明天差點三進宮)**:8/19 slot1 撞 8/14 白鞋、8/20 slot1 撞 8/15 帆布鞋——行事曆「生成時」的冷卻檢查沒有對齊「核准時」的七天實發窗(plan-vs-plan ≠ plan-vs-aired)。短修:逐日換題手術(W-SLOT1SWAP 模式);根修:生成器冷卻檢查改對 as-aired 歷史,排入優化工。

## F23|2026-08-20 01:07-01:22:Codex DPAPI 間歇失效,根因未定(兩次錯誤更正,誠實記錄過程)
- **現象**:D+3 補貨對 8/21 跑 `daily-generate.ps1`,`codex exec -s workspace-write`
  死於 `CryptUnprotectData failed: 2148073483`(Windows 憑證解密失敗)——**三次真實嘗試
  失敗**(兩次背景執行、一次前景執行整支腳本)。
- **排查過程(兩次錯誤結論,如實留底,不事後美化)**:
  第一版判斷「一過性」:單次前景重現簡單無害 prompt 成功,就結案——**錯**,
  下一次背景仍失敗。
  第二版判斷「背景執行是變因」:對照前景 Bash/PowerShell 簡單 prompt 都成功、
  兩次背景都失敗,鎖定背景為根因——**也錯**,隔幾分鐘後**前景跑整支
  `daily-generate.ps1`(真實生成流程,非簡單 prompt)一樣失敗**,而且失敗後
  立刻用 `-s read-only` 簡單 prompt 測試又乾淨成功。
- **目前唯一站得住的觀察**:失敗只發生在**真實生成流程**(完整 skill 執行,
  大量檔案讀寫、多輪工具呼叫)裡的 `-s workspace-write`;**簡單無害 prompt**
  (不管 `-s workspace-write` 或 `-s read-only`、不管前景背景)目前測試全部成功。
  變因可能是流程中某個特定操作(特定檔案寫入?特定子指令?)觸發,不是
  「前景/背景」這種粗粒度的分類——**這一點本身也還沒有被證實,只是目前唯一
  沒被證偽的假說**。
- **決定**:停止盲目重試(已消耗三次真實嘗試,CLAUDE.md 鐵則:資源型失敗後
  禁止擴大範圍)。8/21 補貨**今晚不再嘗試**,改為監看幾小時後**真正的 06:30
  排程**是不是也中招——那是唯一不用再猜測、能給出確定答案的時窗。
- **對 06:30 真排程的風險評估上修為:不明,不是低**。前一版「背景執行才會中」
  的假說已被前景失敗證偽,原本「Task Scheduler 用不同機制所以安全」的推論
  失去立論基礎。**這是懸而未決的真實風險,不是已排除的風險。**
- **教訓**:🔴 對間歇性故障,**一次成功不能當作結案證據**,只能當作「這次沒重現」。
  兩次過早結案都栽在同一個模式:拿到一個乾淨的正面結果就急著寫「根因鎖定」。
  真正的根因要嘛等它自己說清楚(更多次對照),要嘛等到能長期穩定復現的最小案例。
- **【2026-08-20 07:1x-07:3x 定案補記】機轉釘死了,「真實流程 vs 簡單 prompt」是紅鯡魚**:
  ①真排程實證:06:30 被 F24 守衛擋掉沒碰到 Codex;07:13 用 `Start-ScheduledTask`
  手動觸發(Task Scheduler 自己的執行環境),Codex 每一條指令同樣死於
  `codex_core::exec: windows sandbox: CryptUnprotectData failed: 2148073483`。
  **排程環境也中,徹底排除「呼叫方式」假說。**
  ②鑑別實驗(極簡 prompt+一條 `echo`):`-s workspace-write` ❌、`-s read-only` ❌、
  `-s danger-full-access`(無沙箱)✅。**真正變因=Codex 沙箱是否執行任何 shell 指令**。
  昨晚「簡單 prompt 全成功」是因為那些 prompt 沒跑過指令;「真實流程必死」是因為
  第一條指令就死。工作量大小從頭到尾無關。
  ③Codex 能思考、能回話、能用內建影像模型(不經沙箱 exec)——只有沙箱 exec 層壞。
  ④**這是復發**:7/29 同錯發生過(`generate-missing-images.ps1` 開頭註解與
  fabcb006 同日寫入;codex-cli 也在當天 22:05 重裝),之後三週天天正常,
  8/19 06:44 排程還成功、8/20 01:07 起全滅。codex-cli 版本(0.144.1)三週未變,
  壞的是機器狀態(嫌疑:Windows 更新動了 AppContainer/DPAPI;0x8009000B=
  NTE_BAD_KEY_STATE)。
  ⑤**生產不必等修**:7/29 留下的繞道還能用——行事曆/manifest 由總指揮直跑 npm,
  圖片走 `generate-missing-images.ps1`(read-only 餵 prompt、不叫沙箱跑指令、
  腳本自己搬檔蓋章)。8/21 已用此路開工。
  ⑥交給老闆的修復選項:重開機(NTE_BAD_KEY_STATE 常見解)、重裝 codex(7/29 前例)、
  或暫時維持無 Codex 繞道。`-s danger-full-access` 可用但等於拆沙箱,不建議常態化。

## F24|2026-08-20 凌晨:深修哲學的碼二度回寫生產樹+自鎖(F22 復發變種)
- **現象**:01:59-02:17(戰情室 01:54 收工提交**之後**)有一條線把 9 個檔案寫進
  生產樹未提交:5 支排程腳本(watchdog 系列/daily-generate/sentinel)改成
  fail-closed 哲學(停用任務不自動重啟、髒樹直接擋不 stash)、`gbpPost.ts`/
  `postStory.ts` 加發布回讀驗證、新增 `schedulerSafety.test.ts`。
  06:30 排程執行的是磁碟上這個未提交版,**它自己的新守衛看到它自己未提交,
  把整夜生成擋掉**(「BLOCKED: uncommitted production-code change(s)」)。
- **歸因**:寫入者不在 CCD session 紀錄裡(全文搜尋零命中),profile 完全是
  深修線的發布真實性哲學;`stash@{0}`(8/19 06:30 舊看門狗 stash 走的 12 檔
  +2589 行大包)是同一條線更早的一波。**「深修永遠在分支」(F22)第二次被違反。**
- **處置**:照 F22 判例,整包保全到分支 `scheduler-safety-wip-20260820`
  (commit 6bf50ea,已推遠端,未審未合),main 磁碟還原為已提交版,守衛視角清空。
  改動本身品質看起來不差(fail-closed 哲學跟老闆的安全觀一致),**值得審後收編,
  但收編是審查後的決定,不是撿到就用**。
- **教訓**:守衛這次是功臣——它照設計把未審碼擋在排程外,代價是一夜生成。
  真正的病是**寫入紀律**:深修線需要一個機械閘阻止它寫 main 工作樹
  (它顯然不讀交接文)。

## F25|2026-08-20 晨:slot1 計畫強制只活在 Codex prompt 層(直跑生成必自由選題)
- **現象**:Codex 沙箱壞死後改直跑 `npm run generate -- --date 2026-08-21`,
  slot1 自由選出「包包提把」——跟 8/20 slot2(當晚發)同物件,隔天 10:20 核准
  必被七天防重閘擋(缺格)。追查發現 `slot1-plan.json` 的計畫強制**只存在於
  daily-generate.ps1 注入 Codex 的 prompt 文字裡**,TypeScript 生成器
  (`generateDailyContent`/`contentPlan`)完全不讀計畫檔。
- **時間差漏洞**:七天防重閘在生成時讀 posted-log,**看不到當晚還沒發的貼文**
  ——生成 8/21 時(07:44)今晚 20:30 的提把文尚未入帳,閘形同虛設;
  到隔天核准時它才擋,但那時已缺格。
- **處置**:8/21 已派換題手術(BOARD0821-W-SLOT1SWAP,同款手術第三刀);
  真正的修法=把計畫讀取+「已排未發」視窗納入 buildDailyContent 本身,
  讓任何生成路徑(Codex/直跑/未來的任何 orchestrator)都吃到同一套強制。
- **教訓**:**閘門住在 prompt 裡=只保護走那條 prompt 的路**。防禦要住在
  被所有路徑共用的最深層(這裡=contentPlan),跟 F21「寫入走 writeDailyContent」
  同一個道理。
- **【8/20 上午 addendum】slot2 也在 F25 病灶範圍,而且會吐「無物件題」**:
  直跑生成的 8/22 slot2 出「客人最常忽略的是內裡和邊角」、8/23 slot2 出
  「每十天公開一次洗護觀察」——細節拆解模板的物件欄連兩天吃到空泛填充語。
  實證後果:護照圖鎖不了件,Codex 影像模型直接拒生成(「缺少主題/指定衣物
  名稱」)——這個拒絕反而是好的 fail-closed,擋住了文不配圖。修法同 F25 本體:
  物件選擇的完整性檢查(拒絕無物件題)應該進 contentPlan,不是靠影像模型
  在最後一里路救。兩天都以換題手術修復(0822-SLOT2FIX 牛仔褲、0823-DAYFIX
  行李箱)。
- **【護照 registry 的粗鎖陷阱,兩次實證】**:泛用 pattern 會吃掉具體物件——
  「抱枕」被 /枕/ 鎖成棉被套、「書包」落到泛用 handbag(與 8/20 提把同鎖)。
  換題手術選新物件時,**先查 OBJECT_SPEC_RULES 會把它鎖成什麼**,鎖不對就
  換物件或(經審)加具體規則,不要硬上。
- **【8/20 根修落地】slot1 計畫強制與時間差視窗都進了 contentPlan 本體**:
  ①`buildDailyContent` 讀 `data/slot1-plan.json`(`applySlot1Plan`,
  generateDailyContent 無條件開啟),計畫題通過 as-aired cooldown 就優先採用,
  slot 結構走與輪播同一條模板鏈(service 由計畫題自己推導,`serviceForTopic`,
  否則制服題會配到白鞋文案);手排 special slot(節慶檔)優先於計畫;計畫題
  踩 cooldown 則 fallback 回輪播並 `console.warn` 進生成 log(`resolveSlot1WithPlan`)。
  ②`recentAiredTopics` 對「還沒播完的日子」(視窗日 ≥ 生成當日)改收行事曆
  全部已排題,不再等 posted-log——「已排今晚未發」從此佔用視窗;已結束的日子
  維持 aired-only,暗日(8/12-13 型)不會回來汙染。
  ③突變實測三刀逐項紅:拿掉計畫讀取 4 紅、拿掉未發視窗 2 紅、拿掉 cooldown
  檢查 2 紅(`test/slot1PlanEnforcement.test.ts`);tsc+vitest 全套 616 綠;
  用主樹真資料重放 8/20→8/21 場景:視窗收到 16 題(含當晚三格未發),計畫題
  「開學前學生制服檢查」正確採用。prompt 注入層不動,留作第二層。

## F26|2026-08-21 凌晨:72 小時成效表寫死 slot 2,量到的是別支貼文(發現於補優化日誌時)

- **現象**:依收工交接文接手補優化日誌,查到 `output/reviews/batch-review-2026-08-20.json`
  判「12 支滿 72 小時的 Reel,0 支通過門檻」,工具自己建議「換 hook 文案」。
  正要照建議動手改 `src/reelConcepts.ts` 的 hook 前,交叉核對 `data/posted-log/`
  發現數字對不上:8/15(down-jacket-cuff,批次表 reach=47)與 8/16(heel-tip-scuff,
  reach=44)那兩天,slot 2 實際發的是**跟這兩個概念完全無關的輪播/單圖**
  (`published_media_type: "carousel"`/`"image"`,`video_deferred_reason: "Video
  file is missing for slot 2"`);而 8/16 當天真正的 Reel(`published_media_type:
  "reel"`)其實發在 **slot 3**。`video-reviews/2026-08-20.json`、`2026-08-23.json`
  也各自把 mattress-pad-sweat、wallet-edge-wear 記在 `"slot": 3`,同樣佐證。
- **根因**:`src/reelBatchReview.ts` 判 Reel 成效寫死
  `content?.slots.find(item => item.slot === 2)` 與 `post.slot === 2`。這在
  8/14 以前是對的(當時 noon=slot3、evening=slot2 兩條 Reel 並行,`REEL_SCHEDULE`
  對應的正是 evening/slot2);但 `data/ab-test-plan.json` 從 **8/15 起 evening
  半場全部標 `paused: true`**,`scheduleReel.ts --heal` 的 `planSlot()` 正確
  跳過被暫停的半場(不補 slot 2 的 Reel),production 因此收斂成一天一支、
  改發在 slot 3——但 `reelBatchReview.ts` 沒人跟著改,繼續認 slot 2,於是
  量到的是「slot 2 那天湊巧發的別支輪播/單圖」的 reach/engagement,誤貼成
  該 REEL_SCHEDULE 概念的成效;概念真正的 slot-3 Reel 反而從沒被這支工具量過。
- **已核實範圍**:交叉抽查 `data/posted-log/2026-08-09.json`(乾淨,slot2=真
  Reel)、`2026-08-14.json`(乾淨,slot2/slot3 各一支真 Reel)、`2026-08-15.json`
  `2026-08-16.json`(污染,slot2=輪播/單圖)、`2026-08-18.json`(污染,slot2=
  單圖)。`ab-test-plan.json` 的 paused 旗標從 8/15 起連續為真,與污染起點精準
  對齊,判定 8/14(含)以前的 10 筆 matured 數據乾淨可信,8/15 起(down-jacket-cuff、
  heel-tip-scuff,及之後每一筆一旦成熟)全部不可信。**尚未逐筆窮舉 7/29-8/13**,
  只抽驗 8/9 一筆;抽驗通過不等於窮舉過。
- **處置**:今晚沒有動 `reelBatchReview.ts`(怕在凌晨無人複核下把計分邏輯改錯,
  這支工具的輸出會餵給未來的內容/排程決策,屬於「會影響放行的東西」)。改為
  ①拿掉靠這份污染數據推導的 hook 文案改法(見下),②寫這篇存證,③用
  `spawn_task` 開一張獨立任務單排查+修復,依專案慣例要過深審。
- **教訓**:凡是「日期→格位」的假設一旦跨系統(`REEL_SCHEDULE`/extension 的
  schedule 陣列 vs `ab-test-plan.json` 的 noon/evening 半場)分別演化,兩邊
  遲早會分岔;量測程式碼不能把「哪個格位是 Reel」寫死,要從當天實際
  `published_media_type === "reel"` 反查,或至少讀 `ab-test-plan.json` 的
  paused 狀態同步判斷該不該信任那個格位。**在拿「數據說話」當優化依據前,
  先問這個數字的來源程式碼今天還對不對**——這正是本回合交接文要求的
  「怎麼驗」,不是套公式,是真的去查。

## F27|2026-08-21 凌晨:差點把已測試過的 hook 敘事結構當「泛用問句」蓋掉

- **現象**:F26 發現前,已經照批次表建議改了 9 個概念的 `hook`(改成直接問句,
  如「先看四個角的邊油」→「四個角邊油還在嗎」),`npx tsc --noEmit` 過、但
  `npx vitest run` 在 `test/reelConcepts.test.ts` 的
  `reel concept story structure (loop + delayed reveal)` 這組測到真紅:
  `makes every hook an unexplained inspection action, not the retired generic
  line`。
- **根因**:這 12 個內建概念的 hook 早就經過一輪迭代——從更早的「泛用退休句」
  (如 `retiredHook: "白鞋泛黃，不是刷得不夠用力"`)改成現行的「未解釋動作 +
  迴圈詞 + 延遲揭曉」結構(hook 只給動作與物件詞、不給診斷詞,診斷詞要壓到
  narration 第二句才出現),而且**有突變測試背書**(貼回舊 hook/narration
  必須讓「delays the reveal」那組測試變紅)。我改的問句式 hook 在多個概念上
  直接漏掉迴圈詞(如 luggage-wheel 漏了「輪」)、甚至把該延遲的診斷詞提前
  寫進 hook 本身(handbag-handle 的「是手汗嗎」直接劇透 narration 才該揭曉
  的「手汗」),等於在不知情下毀掉一個已經驗證過的敘事機制。
- **處置**:9 個 hook 全數字面精確復原(`git diff --stat` 確認與 HEAD 零差異),
  `test/reelConcepts.test.ts` 14/14 轉綠。今晚沒有再嘗試在敘事結構限制內
  重寫 hook——沒有足夠證據支持特定新詞句,勉強擠一版只是為了「今天有改」,
  不是真的有把握的實驗。
- **教訓**:改任何一個「看起來只是文案」的欄位前,**先搜這個欄位有沒有專門
  的敘事/結構測試**(`grep -n "hook" test/*.ts`),不要只憑 `tsc` 乾淨就當作
  安全。F26+F27 同一晚出現不是巧合:兩者都是「先查資料/測試的真實來源,
  再動手」這件事沒有先做完就想動刀。
- **【殘留,已知未修】gram 比對對「物件在句尾的自由題」失明**:
  `topicObjectHead` 取前 8 字,8/20 slot2 的口語題「下班最常背的包先看提把」
  截完剩「下班最常背的包提」,「提把」被切掉,重放實測 gram=NO。生成器自產題
  (計畫/輪播/模板)物件都在句首所以互擋有效;會漏的只有 Codex 自由寫的句尾
  物件題——F25 根修後 slot1 已無自由題,slot2/3 仍有此暴露。動它=動核准閘
  同源演算法(autoApprove 共用),按控制平面規矩另單送審,不順手改。

## F28|2026-08-21 凌晨:junction 路徑跑 tsx CLI = isMain 判假 = 靜默零輸出 exit 0(假成功)

- **現象**:為補產 denim Reel 跑 `produce-next-reel.ps1`,秒退
  「Could not read concept status.」。逐層鑑別:`node -e` 正常、
  `npx tsx --version` 正常、但 `npx tsx src/reelConcepts.ts` **零輸出且
  exit 0**——不是掛掉,是 main() 根本沒跑。同一指令在 Git Bash 下卻正常。
- **根因**:`src/cli.ts` 的 `isMain()` 用
  `resolve(process.argv[1]) === fileURLToPath(metaUrl)` 判斷入口。
  經 junction(`C:\Users\cyc39\laundry-repo` → `Documents\New project 5`)
  呼叫時,`argv[1]` 是 junction 路徑(path.resolve 不解 junction),而
  tsx 的 ESM loader 已把 `import.meta.url` 解成實體路徑——兩者永不相等,
  isMain=false,**每一支 `isMain` 守門的 CLI 都靜默變 no-op、退出碼還是 0**。
  Git Bash 倖免是因為它把 junction cd 解成實體路徑再交給子程序。
- **波及面**:src/ 下所有 `if (isMain(import.meta.url))` 入口(autoApprove、
  scheduleReel、reelConcepts、videoReviewGate…全家)。凡由 junction cwd
  或 junction 絕對路徑發動的 powershell/cmd/排程任務呼叫全部中招;
  我今晚自己註冊的一次性任務第一版就是這樣死的。正牌排程任務
  (register-catchup-task.ps1)一直用實體路徑所以從未發病。
- **處置**:一次性任務改實體路徑後立即復活(狀態讀取成功、預產圖被接受)。
  程式層修法(isMain 兩側都過 realpathSync)動的是**全部 CLI 的共同入口**,
  照控制平面規矩另單送審,不順手改。
  **2026-08-21 治本已落地**(分支 `claude/cranky-golick-af8ebf`,待合併 main):
  `e6d7fbb` isMain 兩側 realpathSync + win32 fold → `99c7fd3` 測試強化 →
  `fa617bb` 終版=**候選拼字交集**(每側取「字面拼字 ∪ realpath 拼字」,
  任一拼字 fold 後相等即同檔)。改版原因:R2 兩席跨家族獨立收斂
  「同層或混層比對都會把單側瞬時 realpath 失敗放大成 F28 靜默 no-op」。
  驗證:tsc 乾淨;isMain 測試 9 條含單側故障注入+oracle;六發突變全紅
  (兩個前代語意各被一條故障測試單獨釘死;.native 繞道被 oracle 抓);
  junction cwd 端對端 A/B=舊碼 0 行 exit 0、新碼 363 行 JSON;36 格矩陣
  **dominance 36/36**(對兩前代零迴歸格,分歧全為 false→true)、
  絕對正確 32/36。三輪複審(grok 紅隊 xhigh ×3 + Claude 深審 ×3)
  終判皆 **PASS_WITH_FOLLOWUPS、程式面零缺陷**,不擋合併。
  已知殘餘(皆部署不可及、不改碼,列此存查):
  (1) 拼字法結構上限 4/36——帶 link 拼字的一側失去 realpath 時交集必空,
  任何拼字比對法皆然,唯 dev+ino 身分比對能關但 Windows 有 (0,0) 退化陷阱;
  (2) win32 fold 誤判 true 家族——需 case-sensitive NTFS 目錄+同名異大小寫
  檔(grok R3 變體再加 symlink 雙胞),全 repo 192 項 0 碰撞;
  (3) POSIX 側「不 fold」不變量無斷言(win32-only 部署);
  (4) SUBST 未實測(兩側同函式的對稱性論證應安全)。
  另:`data/.gitignore` 未追蹤且被 root .gitignore 擋掉,全套 633 綠在
  乾淨 clone 不可攜——與本修無關,另案處理。
- **教訓**:**「退出碼 0 + 無輸出」是這個 repo 特有的假成功形態**,遇到
  CLI 無聲就先問「我是不是從 junction 呼叫的」。跑 npm/tsx 一律用
  `C:\Users\cyc39\Documents\New project 5`(實體),junction 只留給
  「路徑不能有空格」的場合(如 dispatch.ps1 的 -Cwd,那是另一個 bug,
  同晚發現:含空格 cwd 會被拆成流浪參數)。

## F29|2026-08-21 凌晨:standing-policy 中繼資料寫入器盲目整筆替換,毀掉既有雙審 REJECT

- **現象**:produce-next-reel 重排 8/23(wallet)時照例跑
  `owner-video-review --standing-policy`,把 `data/video-reviews/2026-08-23.json`
  裡我 8/20 寫的兩席 REJECT(含完整退件理由)整筆換成全 pending 的中繼資料
  ——判決歷史直接蒸發。發布閘仍 fail-closed(pending ≠ pass 照樣擋),
  但「這支被排上的片為什麼不能播」的記錄不見了。
- **根因**:中繼資料寫入器的寫法是「同 slot 舊記錄全過濾掉、推入新記錄」
  (與 videoReviewGate.recordVideoReview 同款 replace 語意),沒有
  「既有記錄已是 rejected/approved 就保留判決欄位」的合併邏輯。
- **處置**:REJECT 記錄以原文復原並加 RESTORED 註記。寫入器的修法屬審核
  記錄鏈(會影響放行歷史的東西),另單送審,不順手改。
  → 修法已落在 `claude/priceless-wright-e8231a` 分支(4 commits,e30cbd5..80b3e98):
  standing-policy 改 merge 語意——同 slot 既有 approved/rejected 且 sha+prompt hash
  未變時只刷新排程戳,判決欄位原樣保留;資產真的換了才重設 pending,舊判決附進
  `superseded`;`--watched` 也把被置換的判決/歷史折進 trail;同指紋多筆判決取
  最後一筆(時序律);髒 status/非陣列歷史不再蒸發。跨家族兩席四輪審畢:
  luna 深審 PASS_WITH_FOLLOWUPS(新缺陷 0)、grok 紅隊全鏈 CLOSED/RESIDUAL_AGREED
  (runs F29CLOBBER-RA v2-v5、RB v1-v4)。測試 21 條含六刀突變實測,全套 638 綠。
  殘留(兩席同意列後續單,詳 `_bridge\materials\F29CLOBBER\DISPOSITION.md`):
  ①三寫入器無跨程序鎖②閘門 find 第一筆、不拒重複 slot、不讀 superseded
  ③videoReviewGate.recordVideoReview 仍整筆 replace④人工手寫 SOP⑤同指紋曾
  REJECT 換回時不自動抬回(老闆裁決題)。重複判決選現行這一類經 grok 類別終判
  「寫入端關不完」,整包走閘門拒收重複+寫入 schema 的後續單。
  → 2026-08-21 10:15 修法併入 main(merge 19e2a52、已推 origin)後,8/23 REJECT
  依 BOARD0823-R-REELREVIEW 存證二度復原(機器欄無存證者維持 pending 不自貼),
  並實跑同一條 standing-policy CLI 驗證:回 preserved_existing_verdict=true、
  判決原樣、僅排程戳刷新——輾掉它兩次的指令現在保住它。
- **教訓**:**重排=重寫中繼資料**,任何人工判決寫進 video-reviews 後,
  只要該日再被 schedule 一次就會被清掉——在修好之前,重排過的日子要回頭
  檢查判決還在不在(本 session 已中招一次:8/22 tC 的 REJECT 也是寫在
  standing-policy 之後才安全的)。
