# 私享家 2026-09-04 交付驗證包(給第三方模型/人工複驗)

> 目的:讓另一個模型或人不靠我的自述,獨立驗證今天做的每一件事。
> 每項都給「怎麼查」「預期看到什麼」「查不到代表什麼」。所有路徑相對於生產樹
> `C:\Users\cyc39\Documents\New project 5`(= junction `C:\Users\cyc39\laundry-repo`)。

## 0. 一句話

早上 session 是被**歸檔**不是刪除;發布排程獨立於對話,今天 11:30 / 12:00 / 13:00(YT)三格平台端都驗到實體;
SEO 三項站內修改 + Hermes 未提交碼落地 = **PR #38**;基底分支本來就紅的 schedule-ahead 缺陷 = **PR #39**;
IndexNow 88 版已補送;9/3 GA4 已補收;9/3 YT 補傳被閘門正確擋下(未繞過)。

---

## 1. 兩個 PR(目標分支都是 `still-material-optics`,不是 `main`)

| PR | 內容 | 驗證指令 | 預期 |
|---|---|---|---|
| [#38](https://github.com/39211/laundry-social-auto-poster/pull/38) | commit A:Hermes 內鏈重平衡 + sitemap 成交頁置前 + 重產 `docs-internal/public-image-metadata.json` + 新增 `scripts/regen-public-image-metadata.mjs`<br>commit B:「西屯洗鞋」單一著陸頁、價目表升導覽第一位、首頁露三行參考價、鞋包頁 title/H1 改整句問句 | `git fetch origin && git checkout claude/seo-money-page-rebalance`<br>`npx tsc --noEmit`<br>`PUBLIC_SITE_BASE_URL=https://sixiangjialaundry.com PUBLIC_IMAGE_BASE_URL=https://sixiangjialaundry.com META_ACCESS_TOKEN=x FB_PAGE_ID=x IG_USER_ID=x npx vitest run test/publicSite.test.ts test/lineAttribution.test.ts` | tsc 無輸出;35 + 10 全過 |
| [#39](https://github.com/39211/laundry-social-auto-poster/pull/39) | `scheduleAhead.ts` 一個 hunk:Reel 格影片不可發布時改成 `skipped` 列,不再讓整天 abort | 同上 env,`npx vitest run test/scheduleAhead.test.ts test/postCurrentSlot.test.ts` | 31/31 |

**為什麼目標是 `still-material-optics`**:它比 `origin/main` 多 38 個 commit,含 `16132772` 這種 main 沒有的原始碼;生產樹就 checkout 在它上面。對 main 打 patch 會在 `generatePublicSite.ts:7444` 附近失敗(實測)。

**兩個 PR 的堆疊關係(13:5x 更新)**:`still-material-optics` 有兩條本來就紅的測試——A = image-metadata 一致性(#38 修)、B = schedule-ahead 延期 Reel(#39 修)。單獨開時各背對方那條,所以 #39 已 rebase 到 #38 的分支上(base 改為 `claude/seo-money-page-rebalance`),#39 的 CI 應全綠;#38 仍背 B 那條紅。**合併順序:#38 → #39**(#38 合併後 GitHub 會自動把 #39 的 base 轉回 `still-material-optics`)。

**堆疊後 CI 實績**:PR #39(= #38 兩個 commit + #39 一個 commit)的 CI run [33843155178](https://github.com/39211/laundry-social-auto-poster/actions/runs/33843155178) → `typecheck-and-test` **pass**,**99 檔 / 838 過 / 16 skipped / 0 紅**。這是 `still-material-optics` 自 9/3 21:09 以來第一個全綠的 CI。驗證者可直接看那個 run 的 log,不用信我。

**PR #38 的 CI 紅是基底繼承的**:
- `git checkout 62c89b1e --detach` 後跑 `test/scheduleAhead.test.ts` → 同一條紅,錯誤字串相同
- `gh run list --branch still-material-optics --limit 5` → 5 次全 failure(9/3 21:09 起)
- `gh run list --branch main --limit 3` → 全 success
- PR #39 就是修這條;合併後 #38 的 CI 應轉綠(重跑即可)

**突變實測**(證明新斷言有鑑別力,不是自貼標籤):
- 把 `headerNavServices().map(` 改回 `SERVICE_PAGE_DEFINITIONS.map(` → 新測試紅(`expected 'https://…' to be 'https://…/taichung-laundry-price-list.html'`)
- 把首頁 `<h1>` 改回「台中免費收送,逢甲・西屯洗鞋先看材質」→ 新測試紅(`not to contain '西屯洗鞋'`)

**合併後必做**:生產樹上還有 Hermes 那份未提交的同兩檔修改。先
`git checkout -- src/generatePublicSite.ts test/publicSite.test.ts` 再 pull,否則衝突。

---

## 2. 今天發布(三源驗證,不信自家帳本)

| 格 | 來源 1 posted-log | 來源 2 catch-up log | 來源 3 平台鐵證 |
|---|---|---|---|
| 11:30 slot 1 | FB `102911372343400_1067430699373412` / IG `18085326083680694` success | `{"slot":1,"posted":"18004682480988261"}` | IG Graph API:`2026-09-04 11:31:29 CAROUSEL_ALBUM 高跟鞋跟磨損,送洗前先看三個位置。` |
| 12:00 slot 3 | FB `2146378565956549` / IG `18418681726159335` success | `{"slot":3,"posted":"18625525648060711"}` | IG Graph API:`2026-09-04 12:01:21 VIDEO 娃娃能洗,但洗法差很多。` |
| 13:00 YouTube | `data/youtube-log/2026-09-04.json`:`video_id FdpbVFg3il8` | `output/youtube-logs/2026-09-04.log`:`Slot 3 uploaded to YouTube.` | 打開 https://www.youtube.com/shorts/FdpbVFg3il8 |
| 20:30 slot 2 | 尚未到時 | — | — |

**怎麼自己跑來源 3**:repo 內 `scripts/ig-media-dates.mjs`(讀 `.env` 的 META_ACCESS_TOKEN + IG_USER_ID,打 `/media?fields=timestamp`,按台北日期歸日)。
`cd "C:\Users\cyc39\Documents\New project 5" && node <path>\ig-media-dates.mjs`
預期:9/2 之後直接跳到 9/4(9/3 整天沒有,永久)。

**已知瑕疵**:今天 YT 那支標題「先看五官是繡是黏」與影片內容(娃娃能洗)不符——早上 session 已標記,建議退件。

---

## 3. 排程與環境

| 項目 | 狀態 | 查法 |
|---|---|---|
| 19 個 `Laundry-*` 任務 | 全 Ready | `Get-ScheduledTask \| ? TaskName -like 'Laundry-*'` |
| `Laundry-Daily-Generate` 今天 06:30 exit=1 | 根因 `.git/index.lock` 競態(06:31),09:41 手動補上;**競態本身未修**(使用者選「先不修」) | `output/daily-generate-logs/2026-09-07.log` 第 2443–2695 行 |
| `Laundry-GA4-Collect` / `Nightly-Optimize` 9/3 23:10 回 `3221225786` | 死因查不到:log 檔沒生成(死在第一行前)、機器沒睡、TaskScheduler Operational log **關閉** | `Get-ScheduledTaskInfo` |
| TaskScheduler Operational log | **仍關閉**。UAC 提升兩次都回 canceled,我這個 shell 無法提升 | 管理員 PowerShell 跑:`$l=Get-WinEvent -ListLog 'Microsoft-Windows-TaskScheduler/Operational'; $l.IsEnabled=$true; $l.MaximumSizeInBytes=52428800; $l.SaveChanges()` |
| 9/3 GA4 | 已補:2 sessions、0 AI、0 organic(`data/insights/ga4-traffic/2026-09-03.json`) | — |
| 9/3 SEO 判定 | **永久 INCONCLUSIVE**:閘門只認當日 23:10/23:15 那輪,不吃隔天補的(實測重跑仍 BLOCKED,這是閘門正確) | `output/operations/seo-exposure-review-2026-09-03.json` |
| 9/3 nightly_optimize 報告 | **無法補**:腳本沒有日期參數 | `scripts/nightly_optimize.py` |
| `maintenance-suppress.json` | 8/30 設的壓制窗(停 Daily-Generate + Schedule-Ahead 到 9/3)已於 9/4 00:00 到期;「Owner review after 9/3」尚未有人做 | `data/maintenance-suppress.json` |
| `Laundry-Schedule-Ahead` | 上次跑 8/29,今晚 21:40 一週來第一次;D+1..D+3 輸入(9/5–9/7)已逐日查:mp4 在、審查 approved、FB 核准齊 → 安全 | `ls docs/assets/2026-09-0{5,6,7}/slot-03.mp4`;`data/video-reviews/`、`data/approved-log/` |

---

## 4. IndexNow

- 今天 04:31 送過 180 版(200);05:30、09:41 是 182/183 版但 `skipped`
- 12:25 砍到 88 版後**沒人送**(Hermes 自述「已通知 88 URL」為假;`output/operations/indexing-push-state.json` 最後一筆 04:52、sha 對 180 版)
- 13:0x 我跑 `npm run submit-indexnow -- --live` → `IndexNow submitted: 88 canonical HTML URLs`
- 限制:`submitIndexNow.ts` 不寫狀態檔,證據只有 stdout

---

## 5. YouTube 補傳 9/3:被閘門擋下,**未繞過**

`npm run post-youtube -- --date 2026-09-03 --slot 3` →
`{"skipped":"no IG live reel for 2026-09-03 slot 3; YouTube waits for Instagram"}`

`postYouTube.ts:224-236` 寫死「IG 沒有 live Reel 就不上 YT」,沒有 override 旗標。繞過的唯二方法是改碼或偽造 posted-log,兩者都不做。
建議:接受 9/3 在 YT 也空(與「9/3 放棄」一致)。若要改政策,另開 PR 討論。

---

## 6. Hermes 自述六項逐條驗證結果

| 自述 | 判定 |
|---|---|
| sitemap 183→88、價目置前 | 真(commit `62c89b1e`;線上前六筆 = 價目→收送→西屯→公司→鞋包→白鞋) |
| 白鞋頁連價目表 | 真(2 錨點) |
| 指南 `data-money-pages` | 真 |
| 每日文「下一步」連價目+收送 | 真 |
| IndexNow 已通知 88 URL | **假**(見第 4 節) |
| publicSite 測項綠 | **假**(33/34,紅在 image-metadata 一致性;已在 #38 修) |

外加:LaundrySEO 三案例數字對得上,但那是 SEO 公司自家行銷頁;Laundry Depot 原文寫「and strengthened the local signals」,「純網頁路徑」是過度解讀。

---

## 7. 我查不到、不編的

- 競品實際排名名次(搜尋工具不給編號)
- 競品外部連結數、網域年齡
- 競品 GSC 數字
- 9/3 23:10 兩支任務被砍的死因(Operational log 關著)
- 早上 session 被歸檔是自動還是手動(app log 只到 8/27)

## 8. 四週驗收指標(已自動收集,不需人工)

1. GSC 品牌詞「私享家」點擊離開 0(目前 22 曝光 / pos 4.1 / 0 點擊)
2. GSC「台中洗衣價目表」開始有曝光(目前 0)
3. GSC「西屯洗鞋」平均排名從 29.5 往前
4. GA4 `google_organic_sessions` 不再天天 0

## 9. 相關文件

- 競品拆解與證據:https://claude.ai/code/artifact/aca95c18-fe41-4572-98eb-02bcc6932aec
- Vault:`03-Projects/sixiangjia-可驗證網頁SEO套用-2026-09-04.md`(Hermes)、`03-Projects/sixiangjia-SEO-GA4曝光診斷-2026-09-04.md`(LISA)
- 早上 session 全文:`C:\Users\cyc39\.claude\projects\C--Users-cyc39-Documents-New-project-5--claude-worktrees-nervous-kare-eb19d6\462dda85-2446-48a3-a9dc-ee4d66f1f90f.jsonl`(18MB,歸檔未刪)
