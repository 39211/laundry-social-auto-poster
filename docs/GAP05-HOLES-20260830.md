# GAP-05(2026-09-01 ~ 09-03)發布缺口獨立程式碼稽核

- 稽核時間:2026-08-30 20:03 Asia/Taipei(UTC+8)
- 稽核者:Cursor Cloud Agent(kimi-k3),Commander 小潘 派遣
- 稽核基準:`main` HEAD `5a66dc8`,分支 `cursor/gap05-holes-audit-dc29`
- 性質:唯讀稽核。**未**發布、**未**使用 `--live`、**未**注入行事曆、**未**寫入 `approved=true`、**未**呼叫 Facebook/Instagram/YouTube Graph API。POS / smart-laundry Next.js 不在範圍內。

---

## 0. 結論摘要(TL;DR)

| 路徑 | 今晚 20:30 台北能否觸發 | 關鍵條件 |
|---|---|---|
| 當日(slot 2, 8/30)即時發布鏈 | 可觸發,但屬正常排程,非 GAP-05 缺口 | 需 8/30 行事曆 + 核准存在;本樹兩者皆無 |
| 9/1–9/3 當日即時發布 | **不能**(今晚) | `postCurrentSlot.ts:711` 同日限制;且無行事曆、無核准 |
| 9/1–9/3 注入 + 平台側預排(Schedule-Ahead) | **能**(若 `Laundry-Schedule-Ahead` 為 Enabled,21:40) | 見 §4-H1,這是 GAP-05 最大殘留洞 |
| 9/2、9/3 行事曆注入(Daily-Generate 06:30, D+3) | 今晚不會;明後早 06:30 會(若任務被重新啟用) | 見 §4-H2 |
| Watchdog 重新啟用繞過 suppress token | **能**(patrol 每 30 分鐘) | 見 §4-H3/H4,token 未覆蓋 patrol 自身兩段 |
| cover-as-success(VIDEO_DEFERRED 誤記成功) | 修補仍在樹內,見 §3 | commit `daff440` 已在 main |

機器本機狀態(排程任務 Enabled/Disabled、`.env` 的 `DRY_RUN`、`data/PAUSED.json`、`data/maintenance-suppress.json`、`data/publishing-policy.json`)全部被 `.gitignore` 排除(`.gitignore:10-16` 的 `data/*`、`.gitignore:5` 的 `.env`),**無法從 git 樹驗證**,必須在店內生產機上核對。本樹觀察到的狀態見 §5。

---

## 1. inject(行事曆/內容注入)路徑證據

「GAP-05 inject 是 GATE」的程式面含義:`data/content-calendar/2026-09-0{1,2,3}.json` 及其素材只能由指揮官注入。會自動產生這些日期的路徑:

1. **`scripts/daily-generate.ps1:19`** — 預設目標日 = 台北今天 +3。8/30 跑 → 生成 9/2;8/31 跑 → 生成 9/3。無行事曆時走 Codex 全量生成(`daily-generate.ps1:251` 的 `& $codex exec ... $prompt`,prompt 組裝於 237-243);有行事曆缺圖時走補圖(184-211)。
2. **`scripts/schedule-ahead-daily.ps1:40-48`** — 迴圈 D+1..D+3,行事曆不存在就 `npm run generate -- --date $date`(46)並補 manifest(47);後續 hermes 生圖(89-91)、`publish-pages`(115)、`auto-approve`(156)、**`schedule-ahead -- --date $date --live`(165)**。今晚(8/30)的 D+1..D+3 = 8/31、9/1、9/2 —— 這條鏈會在今晚一次完成 9/1、9/2 的「注入 + 自動核准 + Meta 伺服器端排程」。
3. **`src/postCurrentSlot.ts:717-720`** — 發布時若當日行事曆不存在會自動 `generateDailyContent`。但同日限制(`postCurrentSlot.ts:711-714`)擋下非當日;核准閘門(`postCurrentSlot.ts:478-493`)擋下未核准;`catchup-publish.ps1:168-172` 在無核准日誌時直接 `exit 0`。所以當日鏈在「無行事曆且無核准」下自我封閉,不構成 9/1–9/3 的提前注入。
4. 背景註解:`src/contentPlan.ts:1294-1302` 說明 slot-1 主旨由 `daily-generate.ps1` 注入 Codex prompt —— 「inject」一詞的由來。

## 2. `--live` 路徑證據

1. **`src/postCurrentSlot.ts:777`** — `--live` → `dryRun=false`;未給旗標 → 回落 `getConfig()`;**`src/config.ts:19` `DRY_RUN` 預設 `true`**;`.env.example:2` 亦為 `DRY_RUN=true`。`catchup-publish.ps1:243` 呼叫 `post-current-slot` **不帶 `--live`**,是否真發取決於生產機 `.env`(git 外,不可由此驗證)。
2. **`src/scheduleAhead.ts:273-276`** — 未帶 `--live` 時強制 `DRY_RUN=true`;帶 `--live` 走 env config。唯一會帶 `--live` 的叫用者是 **`scripts/schedule-ahead-daily.ps1:165`**。
3. live 發布前的閘門(`postCurrentSlot.ts`):暫停刹車 269-271、發布窗 272 + 217-236、單飛鎖 279-304、核准指紋 311-333、圖片 digest 350-381、七日重複文案 390-421、可發布核准(強制核准不算)478-493、同日限制 711-714、竄改拒發 723-727 → `refuseTamperedPublish` 663-690。
4. `scheduleAhead.ts` 的閘門:`assertLiveMetaConfig` 136、可發布核准 169-172、重複文案 85-112、Meta 15 分鐘~29 天窗 161-168、Reel 影片未備妥則跳過(不降級排圖)214-222。**注意:此檔沒有 `readPause` 檢查,也沒有 `content.tampered` 檢查** —— 見 §4-H5。
5. 逃生閥 `ALLOW_OFF_SCHEDULE_PUBLISH`(`postCurrentSlot.ts:222`、711):全 repo 僅測試(`test/postCurrentSlot.test.ts:650,707`)與 `docs-internal/ERROR-BOOK.md:56` 的人工作業程序引用,**沒有任何排程腳本設定它**。
6. YouTube:`src/postYouTube.ts:494-495` 以 `--schedule-ahead` 分流到平台側預排;由 `schedule-ahead-daily.ps1:183-190` 對 slot 2/3 叫用。本次未觸碰。

## 3. cover-as-success(VIDEO_DEFERRED)修補驗證 —— 仍在樹內

修補來源:commit `daff440`(「Separate a pending video gate from a fault, and fix the carousel video type」),已確認為 `main` HEAD 的祖先(`git merge-base --is-ancestor` 通過)。修補點:

1. **`src/generateVideo.ts:112-115`** — 註解明寫:「Hard gate for one planned Reel. Publishing calls this directly so a cover image can never satisfy a Reel slot by itself.」
2. **`src/generateVideo.ts:117-172`** — `validatePublishableReel` 本體:非 reel/mixed-carousel 直接拒絕(122-124)、缺 `local_video_path` 拒絕(125)、缺 HTTPS 公開影片 URL 拒絕(127-129)、檔案不存在或 0 byte 拒絕(131-133)、缺 grok-imagine-video 來源紀錄拒絕(135-142)、缺 `video_prompt` 與新鮮度判定(144-155)、metadata 探針(157-158)、owner 看片核准(159-164)。
3. 呼叫點:**`src/postCurrentSlot.ts:138`**(在 `resolveSlotPublishMedia` 120-155 內);失敗時回傳 `videoDeferred: true` 且 mediaType 降級為 image/carousel(147-154),並以 `classifyVideoFailure`(110-118)區分 `expected`(等閘門)與 `unexpected`(故障)。
4. 記帳面:**`src/postCurrentSlot.ts:170-181`**(`resultToLog`)寫 `video_status: "VIDEO_DEFERRED"` 與實際 `published_media_type`;**518-531** 寫入 repair queue(含 `defer_kind`);preflight 只回報不落 queue(497-516)。
5. 預排側:**`src/scheduleAhead.ts:214-222`** — Reel 槽影片未備妥時**跳過不排**,註解 205-213 記錄 8/25 把靜態圖排進未來 Reel 槽的静默降級事故;`facebookScheduleKind` 69-75 為映射單一真相。
6. 測試釘住:`test/postCurrentSlot.test.ts:135`(mixed 影片缺失 → 發四圖 carousel 並記 VIDEO_DEFERRED)、`:205`(preflight 不寫 repair queue)、`:234`(pending gate vs fault)、`:290`(純圖槽不產生 repair 項)、`test/duplicateVideoGate.test.ts:183`(cover fallback 或 dry run 不算已播出 Reel)。

**結論:封面圖已不可能再被記為影片發布成功;修補完整在樹。**

實測佐證(本機,唯讀):以 CI 同款 placeholder env(`.github/workflows/ci.yml:23-29`)執行 `npx vitest run test/postCurrentSlot.test.ts test/pause.test.ts test/slotTimeSingleSource.test.ts test/duplicateVideoGate.test.ts test/slotImagePlan.test.ts` —— **5 檔 66 測試全綠**(vitest 4.1.6,2026-08-30 20:05 台北)。

## 4. watchdog 重新啟用鏈證據與殘留洞

### 現狀(suppress token 覆蓋面)

- **`scripts/_watchdog.ps1:15-26`**:讀 `data\maintenance-suppress.json`,`until` 未過期則 `return`(抑制);**28-42** 否則把每個 Disabled 的 `Laundry-*` 任務 `Enable-ScheduledTask` 並跳 toast。
- 會 dot-source 它(即受 token 保護)的腳本:`catchup-publish.ps1:19`、`daily-approve.ps1:18`、`day-audit.ps1:18`、`produce-next-reel.ps1:60`、`schedule-ahead-daily.ps1:19`、`watchdog-patrol.ps1:21`、`weekly-review.ps1:24`、`youtube-upload.ps1:39`。

### H1(最大洞):Schedule-Ahead 若在 Enabled,今晚 21:40 就會注入 + 預排 9/1、9/2

`schedule-ahead-daily.ps1` 全鏈(§1-2)會對 D+1..D+3 生成行事曆、生圖、推 Pages、`auto-approve`(156)、`schedule-ahead --live`(165)。Meta 伺服器端排程一旦入隊,**之後把本機所有任務停用也攔不住發布**。該任務不在 `register-catchup-task.ps1` 的註冊清單(64-153)內,是手工註冊(`content-playbooks/2026-08-18-90day-plan-v5.md:68` 記載每日 21:40),所以它的 Enabled/Disabled 狀態只能上機確認。**指揮官「Schedule-Ahead 保持 Disabled」是 GAP-05 的關鍵防線,且必須連同 H3 一起看。**

### H2:Daily-Generate 06:30 的 D+3 注入

`daily-generate.ps1:19` 預設 D+3:8/31 早上會生成 9/3、9/1 早上會生成 9/4。維持 Disabled 到 2026-09-04 即可讓 9/1–9/3 不被自動注入(9/1 已在 8/29 的 D+3 範圍 —— 本樹 `data/content-calendar/` 只有 `2026-08-12.json`、`2026-08-14.json`,無 9/1–9/3,與「任務目前確實沒跑」一致)。

### H3(洞):patrol 的兩段重新啟用不受 suppress token 保護

`watchdog-patrol.ps1:21` dot-source `_watchdog.ps1` 時,token 抑制只讓 dot-sourced 腳本 `return`,**呼叫方(patrol)後續程式繼續跑**:

- **`watchdog-patrol.ps1:24-36`**:dead-trigger 偵測(NextRunTime 為空)→ 呼叫 `register-catchup-task.ps1` 全量重註冊;而重註冊 = `Unregister-ScheduledTask` + `Register-ScheduledTask`(`register-catchup-task.ps1:44-46`、59-60),**Disabled 狀態被重置為 Ready**。風險:Disabled 任務通常回報空 NextRunTime,若如此,patrol 每 30 分鐘就會把「刻意停用的 Daily-Generate」誤判為死觸發器並重新啟用,token 完全攔不到。
- **`watchdog-patrol.ps1:81-87`**:發布窗開著且槽未發 → 直接 `Enable-ScheduledTask` + `Start-ScheduledTask` `Laundry-CatchUp-Publish`,同樣不看 token。

### H4(洞):daily-generate.ps1 自帶的重新啟用迴圈不看 token

**`scripts/daily-generate.ps1:53-57`** 有自己的 `Enable-ScheduledTask` 迴圈,沒有 suppress 檢查。只要 Daily-Generate 本身被任何路徑(含 H3)啟用,它 06:30 第一件事就是把所有兄弟任務(含 Schedule-Ahead)重新武裝。

### H5(較小):scheduleAhead.ts 不查暫停刹車、不查行事曆竄改

暫停刹車覆蓋 `autoApprove.ts:74-86`、`approvePost.ts:44-47`、`postCurrentSlot.ts:269-271`,但 **`scheduleAhead.ts` 沒有 `readPause`**。已核准的未來日在暫停期間仍可被排進 Meta 伺服器端佇列(且入隊後無法由本機撤回發布)。另外 `scheduleAhead.ts` 不檢查 `content.tampered`(`postCurrentSlot.ts:723` 有)。

### 不在洞內

- `publish-sentinel.ps1:50` 直接觸發 `catchup-publish.ps1`(20:50 等),本身不查暫停,但下游 `postCurrentSlot` 的暫停/核准/同日/窗口閘門都在,可接受。
- `day-audit.ps1:98` 只啟動 YouTube 上傳;`day-audit.ps1:102-112` 的 first-comment 只作用於已發布貼文。
- `watchdog-patrol.ps1:41` 與 `catchup-publish.ps1:177` 的 slot 2 = 20:30 與 `src/scheduler.ts:6` 一致,由 `test/slotTimeSingleSource.test.ts:21-23`、36-47 釘住。

## 5. 本樹觀測到的狀態(2026-08-30 20:03 台北)

| 項目 | 狀態 |
|---|---|
| `data/PAUSED.json` | **不存在**(刹車未啟動;且被 gitignore,生產機狀態須上機看) |
| `data/maintenance-suppress.json` | **不存在**於本樹(gitignore;若指揮官已在生產機設定 until=2026-09-04,git 無法佐證) |
| `data/publishing-policy.json` | **不存在** → `autoApprove.ts:93-95` 在本樹會擋下一切核准 |
| `data/content-calendar/` | 僅 `2026-08-12.json`、`2026-08-14.json`;**無 8/30、無 9/1–9/3** |
| `docs/assets/` | 最新到 `2026-08-29`;9/1–9/5 的 Pages 資產提交只存在於 `origin/still-material-optics` 分支,不在 `main` |
| `data/approved-log/`、`data/posted-log/` | 不存在於本樹 |

## 6. 今晚 20:30 台北的觸發判定

現在 20:03,接下來:20:30 slot 2 窗口開(至 00:30,+4h,`postCurrentSlot.ts:230`)、20:35/22:15 `Laundry-CatchUp-Publish`(`register-catchup-task.ps1:88-98`)、20:50 sentinel、每 30 分 patrol、21:40 Schedule-Ahead(若存在且 Enabled)。

1. **8/30 slot 2 本身**:若生產機有 8/30 行事曆 + 核准,20:35 起會正常發布 —— 這是設計行為,不是 GAP-05 缺口。本樹無 8/30 行事曆,在此樹什麼都不會發。
2. **9/1–9/3 當日即時發布**:今晚**不可能**。`postCurrentSlot.ts:711-714` 只准當日;9/1 的最早可能發布時點是 9/1 11:35,且前提是行事曆 + 核准都存在(目前都不存在)。
3. **9/1–9/3 提前注入/預排**:今晚**可能**,唯一路徑是 H1(21:40 Schedule-Ahead)與 H3 連鎖(patrol 重新啟用 → 06:30 Daily-Generate → H4 全員復活)。判定條件在生產機的任務狀態與 token 檔,git 無法判定 —— **上機檢查清單**:
   - `Get-ScheduledTask | ? TaskName -like "Laundry-*" | % { "$($_.TaskName) $($_.State) $((Get-ScheduledTaskInfo $_.TaskName).NextRunTime)" }` —— 確認 `Laundry-Daily-Generate`、`Laundry-Schedule-Ahead` 為 Disabled,且注意 Disabled 任務的 NextRunTime 是否為空(為空 = H3 的 dead-trigger 路徑會把它們當死的重註冊)。
   - `type data\maintenance-suppress.json` —— 確認 `until >= 2026-09-04T00:00:00+08:00`。
   - 考慮 `npm run pause -- --reason "GAP-05 維護至 9/4"` —— 這是唯一同時蓋住核准與當日發布的刹車(`pause.ts` 全文;但注意 H5:它蓋不住 schedule-ahead 的已核准未來日)。

## 7. 最小安全守衛提案(**僅提案,未套用、未測試於本樹**)

> 依任務簡報,稽核只交付報告;以下為可直接套用的最小 diff 草擬,套用權在指揮官。

### G1(對 H3):patrol 在重註冊/救援前重讀 token

`scripts/watchdog-patrol.ps1` 第 21 行 dot-source 之後、第 24 行 dead-trigger 檢查之前,插入同一個 token 判讀(約 8 行):token 有效未過期 → 寫 log 後 `exit 0`。這一行為把 dead-trigger 重註冊與 81-87 的救援啟動一起蓋住。測試:比照 `test/slotImagePlan.test.ts:233-260` 的 source-assertion 模式,新增斷言 `watchdog-patrol.ps1` 中 suppress 檢查出現在 `Enable-ScheduledTask` 與 `register-catchup-task.ps1` 叫用之前。

### G2(對 H4):daily-generate.ps1 改用共用 watchdog

`scripts/daily-generate.ps1:53-57` 的內聯迴圈整段換成一行 `. (Join-Path $PSScriptRoot "_watchdog.ps1")`,與其他八個腳本一致,自動獲得 token 抑制。

### G3(對 H5):scheduleAheadFacebook 加暫停檢查

`src/scheduleAhead.ts` 在 134-138 的 config 断言後加:

```ts
const paused = await readPause(input.root ?? projectRoot());
if (paused) throw new Error(pauseMessage(paused));
```

(import 自 `./pause`;約 3 行 + import。)測試:比照 `test/pause.test.ts:50-62` 的「刹車啟動時核准不得給予同意」,新增「刹車啟動時 schedule-ahead 不得入隊」。

### G4(可選):scheduleAhead 加 `content.tampered` 拒絕

對齊 `postCurrentSlot.ts:723-727`,在 `scheduleAhead.ts:140-141` 行事曆載入後拒絕 tampered 日。

## 8. 本次未變更項目(明確清單)

- 未編輯 `src/`、`scripts/`、`test/` 任何檔案;本分支唯一新增:`docs/GAP05-HOLES-20260830.md`。
- 唯一執行過的命令是唯讀測試(`vitest run`,5 檔 66 測試,全數通過;測試皆用各自暫存 root,未觸碰本 repo 的 `data/`)與 `npm ci`(還原 `node_modules`,gitignored)。
- 未執行任何發布/核准/生成命令;未使用 `--live`;未設定 `ALLOW_OFF_SCHEDULE_PUBLISH`。
- 未寫入 `data/content-calendar/`(9/1–9/3 維持未注入,GATE 仍在指揮官手上)、`approved-log`、`posted-log`、`scheduled-log`、`video-repair-queue`。
- 未建立/刪除 `data/PAUSED.json` 或 `data/maintenance-suppress.json`。
- 未呼叫 Facebook / Instagram / YouTube 任何 API;未讀取或使用任何 token。
- 未觸碰任何排程任務(本機為 Linux,無 Task Scheduler;所有 ps1 均為靜態分析)。
- 未查看 POS / smart-laundry Next.js(依命令排除)。
