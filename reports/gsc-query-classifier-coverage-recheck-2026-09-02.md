# GSC 查詢分類覆蓋重驗（2026-09-02）

## 結果

以目前 `src/gscSeoCandidateAutopilot.ts` 的分類規則，重跑 24 個核心白名單與 12 個第二來源觀測詞，全部 36/36 可歸入鞋／包或衣物／寢具／收送群。這不代表詞有曝光或會帶來收錄，只代表候選分群不會因詞形漏接。

另以最近本站 telemetry 保留的兩個查詢做負向檢查：

| 查詢 | 目前分類 | 判斷 |
|---|---|---|
| `干洗的衣服可以水洗嗎` | 未分類 | 簡體「干洗」是可安全加入的 `乾洗` 同義變體，但需在下一個允許修改 source 的窗口加入測試後再推送 |
| `喜鞋店` | 未分類 | 疑似錯字／品牌型查詢；不把單一含「鞋」字的任意查詢放寬成鞋類候選，避免噪音與錯誤改頁 |

## 控制與下一步

1. 核心白名單 27/27 的現有覆蓋保持不變；本輪不改 `src/`、`scripts/` 或 live HTML。
2. 「干洗」只列為待驗證分類變體，不直接產生內容、不觸發 GSC request、IndexNow 或新 URL。
3. 「喜鞋店」保留為 telemetry 觀察，不作自動候選；若日後再次出現，先做人工 query/page 對照。
4. 下一次 source 變更必須補單元測試、mutation／CI 與獨立複審，再上傳 GitHub；在此之前保持目前 fail-closed 行為。

## 證據界線

GSC `top_query_pages` 是唯一可把查詢直接綁定頁面的第一方證據；公開競品頁與單次搜尋結果只能支持需求型態，不能替代本站曝光、點擊或收錄資料。
