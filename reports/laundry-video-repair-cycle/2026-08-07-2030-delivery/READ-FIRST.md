# Read first

This package is a secret-free owner-review handoff for the 2026-08-07 20:30 Asia/Taipei video-repair cycle.

Start with `report.md`, then compare `queue-snapshot.json`, `audit-summary.json`, `current-run-decode.json`, and `first-frame-integrity.json`.

The package contains no raw MP4, OAuth material, API key, cookie, access token, publication command, or `posted-log` mutation. `VIDEO_DEFERRED` remains open until a later topic-matched replacement video is actually published successfully on both Facebook and Instagram.
